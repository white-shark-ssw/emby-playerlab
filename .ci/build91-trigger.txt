Build91 validation: tiered MDK-only abnormal-media recovery. First premature EOF quarantines the poisoned EOS player and rebuilds a fresh MDK generation on the same UnifiedTransport cache. A second premature EOF rebuilds MDK with FFmpeg software decode first and scoped avformat ignore_err + discardcorrupt. Normal playback keeps VT -> FFmpeg and no global demuxer tolerance. Deployment target remains iOS 15.0.
