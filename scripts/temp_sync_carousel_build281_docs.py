from pathlib import Path
import re

CHECKPOINT = Path('docs/project/current/dev/DEV-home-carousel-drag-smoothness.md')
MODULE = Path('docs/project/MODULE_STATUS.md')
PROJECT = Path('docs/project/PROJECT_STATE.md')
DECISIONS = Path('docs/project/TECHNICAL_DECISIONS.md')
INDEX = Path('docs/project/BUILD_TEST_INDEX.md')

# 1) Current checkpoint: refresh identity, real-device evidence, validation and exact next action.
s = CHECKPOINT.read_text()
s = re.sub(r'^- \*\*Status:\*\* .*$', '- **Status:** Active — Build279 target-device rejects recognizer-type-only replacement: `CAROUSEL≈90 / CAROUSEL PAN≈90 / corrected TOUCH≈80`. Build281 / 0.15.14 is the current CI/IPA-verified direct-vs-frame-latched real-input publication A/B.', s, count=1, flags=re.M)
s = re.sub(r'^- \*\*Current working branch:\*\* .*$', '- **Current working branch:** `diag/home-carousel-pan-latch-build281`.', s, count=1, flags=re.M)
s = re.sub(r'^- \*\*Current exact product source:\*\* .*$', '- **Current exact product source:** `a0bb823f81742c7f08908c0465809a41f9e22b3b`.', s, count=1, flags=re.M)
s = re.sub(r'^- \*\*Current candidate:\*\* .*$', '- **Current candidate:** OnePlayer `0.15.14 (281)` — diagnostic-only `CAROUSEL PAN` with equal gesture-local max-refresh versus `CAROUSEL PAN LATCH` that publishes only the latest real Pan translation at most once per display-link tick; exact-source CI/IPA independently verified; target-device pending.', s, count=1, flags=re.M)

build281_section = '''\n## Build279 target-device real-carousel input result — 2026-08-31\n\nThe user tested Build279 with screen recording off and reported **`CAROUSEL≈90 / CAROUSEL PAN≈90 / TOUCH LAYER≈80`**. The uploaded `OnePlayer-App-1788177649.log` confirms 67 real-carousel Pan sessions and 101 corrected raw-touch sessions. Standard Pan callbacks are not sparse: 300 Pan samples / 233 intervals give a weighted average callback interval of about **6.26 ms** with a 40.76 ms tail. Corrected raw touch gives 1,370 samples / 1,269 intervals at about **8.25 ms** weighted average, yet the system HUD remains only ~80. Normal custom-carousel delivered touch across the 24 logged cadence sessions is materially sparser at about **12.68 ms** weighted average, but denser Pan/raw callback delivery still does not lift final presentation.\n\nConclusion: **recognizer type or callback count alone is not sufficient**. Build279 also had one diagnostic fairness gap: its real-carousel Pan surface did not carry the same gesture-local device-max `CADisplayLink` request that normal CAROUSEL starts after horizontal acquisition. Therefore Build279 rejects naked owner replacement but does not yet isolate equal-refresh direct publication versus frame-aligned publication. PR #283 is closed without merge.\n\n## Build281 / 0.15.14 — equal-refresh direct Pan vs frame-latched publication\n\nBuild281 supersedes Build279 without touching HomeCore, the product `V3HomeCarouselInteractionRecognizer`, transition-state ownership, Build241 `>=500 / >=0.28` release contract, Player/MPV/PiP/Transport/Cache/Emby Session, or any media path. It keeps the standard Pan diagnostic owner and adds exactly one publication-timing A/B:\n\n- `CAROUSEL PAN`: standard `UIPanGestureRecognizer` drives the existing real carousel callbacks immediately **and** starts the same style device-max `CADisplayLink` refresh request for fairness. The link does not drive position in this mode.\n- `CAROUSEL PAN LATCH`: the same Pan samples and max-refresh request are used, but `.changed` only replaces one pending **real** translation. The display link publishes at most one pending value per tick. There is no interpolation, predicted/coalesced render authority, synthetic intermediate position, timer, watchdog, fallback, retry or second transition state. On finger end the final real translation is synchronously published before the unchanged release callback.\n\nExact product source `a0bb823f81742c7f08908c0465809a41f9e22b3b`. Xcode 16.4 exact-source run/job `33398830817 / 99509848648` passed. Artifact `9760543570`, digest `sha256:6af8d484a4c5782eee6d46f670a2091f715b06c28e0d044ed2517af746213c4f`. IPA SHA-256 `4357afce03a6e35ede340a20d91f589f110389302145048073e0913bae8dfadd`; source ZIP SHA-256 `23228a1f084d4d0c46c452f8c8bde2ef9145d3164d6c6f230b2d82e526716c16`. Independent unpack verifies `com.embyplayerlab.app / OnePlayer / 0.15.14 (281)`, `MinimumOSVersion=15.0`, IPA integrity and MinOS audit OK. Draft PR #284 remains unmerged pending target-device A/B.\n'''
if '## Build281 / 0.15.14 — equal-refresh direct Pan vs frame-latched publication' not in s:
    s = s.replace('\n## Scope guard\n', build281_section + '\n## Scope guard\n', 1)

s = re.sub(r'## Acceptance / test procedure\n.*?(?=## Validation state)', '''## Acceptance / test procedure\n\n1. Use iPhone 15 Pro Max / iOS 17.0 with screen recording **off**.\n2. `PIPE CAROUSEL`: normal product owner control; note sustained HUD during continuous horizontal drag.\n3. `PIPE CAROUSEL PAN`: same real tree with standard Pan + equal gesture-local max-refresh request; note sustained HUD.\n4. `PIPE CAROUSEL PAN LATCH`: same Pan/tree/refresh request but latest-real-input publication is display-link aligned; note sustained HUD.\n5. Send one line such as `CAROUSEL 90 / PAN 90 / PAN LATCH 120` plus App log.\n6. Do not judge the diagnostic Pan release feel as final product semantics and do not infer final FPS from internal display-link callback timing.\n\n''', s, count=1, flags=re.S)

s = re.sub(r'## Validation state\n.*?(?=## Next exact action)', '''## Validation state\n\n- Build275 same-package control: real-device `CAROUSEL≈90`, fixed TREE/simple probes = 120.\n- Build277 real-device: valid PAN≈110 / SCROLLVIEW≈110; reported TOUCH value invalid due initialization bug.\n- Build279 real-device: ✅ `CAROUSEL≈90 / CAROUSEL PAN≈90 / corrected TOUCH≈80`; recognizer-type/callback-density alone is not sufficient. PR #283 closed, unmerged.\n- Build281 Code written: ✅ exact source `a0bb823f81742c7f08908c0465809a41f9e22b3b`.\n- Build281 exact-source CI passed: ✅ run/job `33398830817 / 99509848648`.\n- Build281 IPA produced + independently verified: ✅ artifact `9760543570`; artifact digest `sha256:6af8d484a4c5782eee6d46f670a2091f715b06c28e0d044ed2517af746213c4f`; IPA SHA `4357afce03a6e35ede340a20d91f589f110389302145048073e0913bae8dfadd`; source SHA `23228a1f084d4d0c46c452f8c8bde2ef9145d3164d6c6f230b2d82e526716c16`; MinOS 15.0.\n- Build281 target-device direct-vs-latched publication A/B: ❌ pending.\n- Stable/frozen reopened performance task: ❌.\n\n''', s, count=1, flags=re.S)

s = re.sub(r'## Next exact action\n.*\Z', '''## Next exact action\n\nInstall Build281 with screen recording off and compare sustained system HUD under **`PIPE CAROUSEL`**, **`PIPE CAROUSEL PAN`**, and **`PIPE CAROUSEL PAN LATCH`** using long continuous horizontal drags. Provide the App log. If equal-refresh direct Pan stays ~90 but frame-latched Pan materially rises, frame alignment becomes evidence-backed for the next product-semantics A/B; if both remain ~90, reject frame alignment and do not add interpolation/smoothing.\n''', s, count=1, flags=re.S)
CHECKPOINT.write_text(s)

# 2) Module status: one authoritative current row.
s = MODULE.read_text()
new_row = '| Home carousel interaction / presentation cadence | **Active — Build279 real-device rejects recognizer-only fix; Build281 frame-aligned publication A/B CI/IPA verified** | Build279 screen-off result: `CAROUSEL≈90 / CAROUSEL PAN≈90 / corrected TOUCH≈80`. Log evidence shows Pan/raw callback density can already be near/high-refresh while final HUD remains lower, so recognizer type or callback count alone is insufficient. Build281 / 0.15.14 exact source `a0bb823f81742c7f08908c0465809a41f9e22b3b` compares equal-refresh direct Pan against display-link-latched latest-real-translation publication on the same real carousel tree; run/job `33398830817 / 99509848648`; artifact `9760543570`; IPA SHA `4357afce03a6e35ede340a20d91f589f110389302145048073e0913bae8dfadd`; MinOS 15.0. Draft PR #284; real-device pending; no product-owner or smoothing contract accepted. |'
s, n = re.subn(r'^\| Home carousel interaction / presentation cadence \|.*$', new_row, s, count=1, flags=re.M)
if n != 1:
    raise SystemExit('MODULE_STATUS carousel row replacement failed')
MODULE.write_text(s)

# 3) Project state: replace only the current carousel active section; preserve all parallel poster work.
s = PROJECT.read_text()
new_active = '''## Active: Home carousel input/publication diagnosis — Build281 / 0.15.14\n\nBuild275 same-package target-device control remains the key boundary: normal finger-driven `CAROUSEL≈90` while the same full carousel tree under fixed device-max progress and simple DISPLAYLINK/SWIFTUI probes reaches 120. Build279 then tested a standard `UIPanGestureRecognizer` on the real tree and corrected raw touch. The user reports `CAROUSEL≈90 / CAROUSEL PAN≈90 / TOUCH≈80` with recording off. `OnePlayer-App-1788177649.log` shows Pan callback delivery itself is already dense (~6.26 ms weighted average) and corrected raw touch ~8.25 ms, so recognizer type/callback count alone is not the missing 120-FPS condition.\n\nBuild279's Pan probe did not carry the normal gesture-local max-refresh request, so the next A/B corrects that fairness gap instead of promoting Pan replacement. Build281 keeps normal CAROUSEL untouched and compares standard Pan with equal device-max refresh in two publication modes: immediate direct publication versus a display-link latch that publishes at most one latest **real** translation per frame. No interpolation/prediction/synthetic point/timer/watchdog/fallback or duplicate transition owner is present.\n\nBuild281 exact product source `a0bb823f81742c7f08908c0465809a41f9e22b3b`; Xcode 16.4 run/job `33398830817 / 99509848648` success; artifact `9760543570`, digest `sha256:6af8d484a4c5782eee6d46f670a2091f715b06c28e0d044ed2517af746213c4f`; IPA SHA `4357afce03a6e35ede340a20d91f589f110389302145048073e0913bae8dfadd`; source ZIP SHA `23228a1f084d4d0c46c452f8c8bde2ef9145d3164d6c6f230b2d82e526716c16`; independently verified `0.15.14 (281)` / MinOS 15.0. Draft PR #284 is unmerged and target-device direct-vs-latched evidence is pending. Build279 PR #283 is closed unmerged.\n\n'''
pattern = r'## Active: Home carousel input/publication diagnosis — Build279 / 0\.15\.12\n.*?(?=## Completed / frozen: Home carousel interaction)'
s, n = re.subn(pattern, new_active, s, count=1, flags=re.S)
if n != 1:
    raise SystemExit('PROJECT_STATE active carousel section replacement failed')
PROJECT.write_text(s)

# 4) Durable technical decision: append once.
s = DECISIONS.read_text()
if '## D025 — Build279 rejects recognizer-only attribution; Build281 tests frame-aligned real input' not in s:
    s += '''\n\n## D025 — Build279 rejects recognizer-only attribution; Build281 tests frame-aligned real input\n\nBuild279 target-device evidence is `CAROUSEL≈90 / CAROUSEL PAN≈90 / corrected TOUCH≈80` with recording off. Its App log proves standard Pan and corrected raw-touch callbacks can already arrive at high cadence while final presentation stays below 120. Therefore changing only the recognizer class, or maximizing callback count, is rejected as a sufficient explanation/fix. Do not promote `UIPanGestureRecognizer` to product owner from Build279.\n\nBuild279 also exposed one fairness gap: the real-carousel Pan probe lacked the normal gesture-local device-max `CADisplayLink` request. Build281 corrects that gap and isolates publication timing on the same real tree. Direct Pan and frame-latched Pan both request device max; the latched mode stores only the newest real translation and publishes at most once per display-link tick, flushing the final real translation before unchanged release handling. This is a diagnostic of frame alignment, not authorization for interpolation, predicted/coalesced rendering authority, synthetic positions, timer/watchdog/fallback, or a second transition state. Product behavior may change only if target-device Build281 evidence supports it.\n'''
DECISIONS.write_text(s)

# 5) Build/Test index: replace Build279 row if present, insert Build281 immediately after it.
s = INDEX.read_text()
b279 = '| **Build279 / 0.15.12** | Real-carousel standard Pan input A/B | **Target-device tested; recognizer-type-only replacement rejected; not stable.** Screen-off HUD: `CAROUSEL≈90 / CAROUSEL PAN≈90 / corrected TOUCH≈80`. App log shows 67 real-carousel Pan sessions with ~6.26 ms weighted callback interval and 101 corrected raw-touch sessions at ~8.25 ms, yet presented FPS remains below 120. Pan density alone is insufficient. Exact source `94f7a4ba5aa5bfdd3beb724e216015141dfc50b7`; run/job `33377860245 / 99443301805`; artifact `9752649942`; IPA SHA `36f17a02a0deddbae593f137d455cba3d22849bf28f00c0fafeb755b620abf9b`; MinOS 15.0. PR #283 closed unmerged. |'
b281 = '| **Build281 / 0.15.14** | Equal-refresh direct Pan vs frame-latched latest-real-input publication | **Code/CI/IPA independently verified; target-device pending; diagnostic-only, not stable.** Keeps normal CAROUSEL unchanged. Both Pan modes request device-max refresh; LATCH publishes at most one newest real translation per display-link tick with no interpolation/prediction/synthetic point. Exact source `a0bb823f81742c7f08908c0465809a41f9e22b3b`; run/job `33398830817 / 99509848648`; artifact `9760543570`, digest `sha256:6af8d484a4c5782eee6d46f670a2091f715b06c28e0d044ed2517af746213c4f`; IPA SHA `4357afce03a6e35ede340a20d91f589f110389302145048073e0913bae8dfadd`; source SHA `23228a1f084d4d0c46c452f8c8bde2ef9145d3164d6c6f230b2d82e526716c16`; MinOS 15.0; Draft PR #284. |'
if re.search(r'^\| \*\*Build279 / 0\.15\.12\*\* \|.*$', s, flags=re.M):
    s = re.sub(r'^\| \*\*Build279 / 0\.15\.12\*\* \|.*$', b279, s, count=1, flags=re.M)
    if b281 not in s:
        s = s.replace(b279 + '\n', b279 + '\n' + b281 + '\n', 1)
else:
    # Insert after Build277 if Build279 has not yet been indexed.
    m = re.search(r'^\| \*\*Build277 / 0\.15\.10\*\* \|.*$', s, flags=re.M)
    if not m:
        raise SystemExit('BUILD_TEST_INDEX insertion anchor missing')
    insert_at = m.end()
    s = s[:insert_at] + '\n' + b279 + '\n' + b281 + s[insert_at:]
INDEX.write_text(s)

print('updated carousel Build279/281 project docs')
