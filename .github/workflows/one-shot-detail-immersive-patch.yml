name: One-shot detail immersive patch

on:
  push:
    branches: [chatgpt/detail-immersive-toggle-navigation-race-20260811]

permissions:
  contents: write

# Temporary staging file; current default-branch Validate Source removes it after applying the real patch.
jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, '[detail-immersive-patched]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chatgpt/detail-immersive-toggle-navigation-race-20260811
          fetch-depth: 0
      - name: Patch root and detail viewport
        run: |
          echo 'Staging only; default-branch Validate Source controller applies this patch.'
