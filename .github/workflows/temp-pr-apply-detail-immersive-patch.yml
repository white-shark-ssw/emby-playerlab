name: Finalize Build199 Project Docs Registered

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  finalize:
    if: github.event.head_commit.message == 'Run registered Build199 documentation finalizer'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Finalize project documentation
        run: python3 scripts/temp_finalize_build199_docs.py
      - name: Commit final state
        run: |
          set -euo pipefail
          rm -f .github/build199-docs-trigger
          rm -f .github/workflows/temp-pr-apply-detail-immersive-patch.yml
          git config user.name white-shark-ssw
          git config user.email houstools@gmail.com
          git add -A
          git diff --cached --check
          git commit -m "Finalize accepted Build199 project state"
          git push origin HEAD:main
