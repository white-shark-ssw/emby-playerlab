name: Build OnePlayer 0.13.22 MDK Direct HTTP Diagnostic

on:
  pull_request:
    branches: [feat/recovery-isolation-0.13.17]

concurrency:
  group: mdk-direct-http-silent-eof-01322-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  validate:
    if: github.head_ref == 'validate/mdk-direct-http-silent-eof-0.13.22'
    runs-on: macos-15
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/recovery-isolation-0.13.17
          fetch-depth: 0

      - name: Materialize and audit Build89 direct HTTP diagnostic
        run: |
          set -euo pipefail
          python3 scripts/materialize_mdk_direct_http_silent_eof_01322.py
          git diff --check
          grep -Fq 'swift_mdk.setGlobalOption(name: "io.avio", value: 1)' MDKLab/App/MDKKSAVIOPlayerEngine.swift
          grep -Fq 'mode=direct-http302-native-avio localhostBypassed=true' MDKLab/App/MDKKSAVIOPlayerEngine.swift
          grep -Fq 'requestHeaders=none nativeAVIO=true nasMediaProxy=false' MDKLab/App/MDKKSAVIOPlayerEngine.swift
          ! grep -Fq 'let server = TransportHTTPServer(session: sharedTransportSession' MDKLab/App/MDKKSAVIOPlayerEngine.swift
          grep -Fq 'mainNativeCall=false' MDKLab/App/MDKKSAVIOPlayerEngine.swift
          grep -Fq 'source=cached-native-snapshot' MDKLab/App/MDKKSAVIOPlayerEngine.swift
          grep -Fq 'action=hold-resolve-only-for-mdk-direct' Sources/Player/PlayerController.swift
          grep -Fq 'action=release-for-mpv-fallback' Sources/Player/PlayerController.swift
          grep -Fq 'engineSwitchNotice = "自动切换为 MPV 高兼容引擎"' Sources/Player/PlayerController.swift
          grep -Fq 'Task.sleep(nanoseconds: 2_000_000_000)' Sources/Player/PlayerController.swift
          grep -Fq '.fill(.ultraThinMaterial)' Sources/UI/PlayerScreen.swift
          grep -Fq '.lineLimit(1)' Sources/UI/PlayerScreen.swift
          ! grep -Fq 'statusBanner(title: "疑似提前结束"' Sources/UI/PlayerScreen.swift
          grep -Fq '@Published private(set) var prematureEOFMessage: String?' Sources/Player/PlayerController.swift
          grep -Fq 'EOFRecovery' Sources/Player/PlayerController.swift
          grep -Fq 'MARKETING_VERSION: "0.13.22"' project.mdklab.yml
          grep -Fq 'CURRENT_PROJECT_VERSION: "89"' project.mdklab.yml
          grep -Fq 'IPHONEOS_DEPLOYMENT_TARGET: "15.0"' project.mdklab.yml
          grep -Fq 'sourceVersion = "0.13.22"' Sources/Core/AppIdentity.swift
          test "$(git hash-object Sources/UI/BufferedTimelineSlider.swift)" = '4c0c772d104fb6b1537e9d89604acbc563114f62'

      - name: Select Xcode and tools
        run: |
          set -euo pipefail
          sudo xcode-select -switch /Applications/Xcode_16.4.app
          brew install xcodegen
          sudo gem install cocoapods -v 1.16.2 --no-document
          xcodebuild -version
          swift --version

      - name: Generate project and install dependencies
        run: |
          set -euo pipefail
          python3 scripts/generate_oneplayer_icons.py
          xcodegen generate --spec project.mdklab.yml
          pod install --repo-update
          xcodebuild -resolvePackageDependencies -workspace EmbyPlayerLab.xcworkspace -scheme EmbyPlayerLab -clonedSourcePackagesDirPath .spm-cache

      - name: Build OnePlayer 0.13.22 Build89
        id: release_build
        continue-on-error: true
        run: |
          set -euo pipefail
          rm -rf build
          xcodebuild -workspace EmbyPlayerLab.xcworkspace -scheme EmbyPlayerLab -configuration Release -sdk iphoneos -destination 'generic/platform=iOS' -derivedDataPath build/DerivedData -clonedSourcePackagesDirPath .spm-cache IPHONEOS_DEPLOYMENT_TARGET=15.0 CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY='' DEVELOPMENT_TEAM='' clean build 2>&1 | tee build.log

      - name: Upload release diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: OnePlayer-0.13.22-build89-mdk-direct-http-release-diagnostics
          path: build.log
          if-no-files-found: warn
          retention-days: 7

      - name: Report compiler failure
        if: steps.release_build.outcome == 'failure'
        run: |
          set -euo pipefail
          grep -nE '(^|[[:space:]])(error:|fatal error:)|BUILD FAILED|SwiftCompile|CompileSwift|EmitSwiftModule' build.log | tail -300 || true
          tail -160 build.log || true
          exit 1

      - name: Validate app identity and minimum OS
        run: |
          set -euo pipefail
          APP_PATH="$(find build/DerivedData/Build/Products/Release-iphoneos -maxdepth 1 -name '*.app' -print -quit)"
          test -n "$APP_PATH"
          test -d "$APP_PATH"
          python3 scripts/validate_built_app.py "$APP_PATH" --bundle-id 'com.embyplayerlab.app' --version '0.13.22' --build '89' --display-name 'OnePlayer' --minimum-os '15.0' --primary-icon 'OnePlayerIcon' --alternate-icon 'OnePlayerAltIcon' | tee app-validation.log
          chmod +x scripts/check_min_os.sh
          scripts/check_min_os.sh "$APP_PATH" '15.0' '17.0' min-os-report.log build.log

      - name: Package unsigned IPA
        run: |
          set -euo pipefail
          APP_PATH="$(find build/DerivedData/Build/Products/Release-iphoneos -maxdepth 1 -name '*.app' -print -quit)"
          rm -rf Payload
          mkdir Payload
          cp -R "$APP_PATH" Payload/
          IPA='OnePlayer-0.13.22-build89-mdk-direct-http-silent-eof-unsigned.ipa'
          /usr/bin/zip -qry "$IPA" Payload
          /usr/bin/unzip -t "$IPA" >/dev/null
          /usr/bin/shasum -a 256 "$IPA" > "$IPA.sha256"
          cat "$IPA.sha256"

      - name: Commit validated Build89 source to experiment branch
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add MDKLab/App/MDKKSAVIOPlayerEngine.swift Sources/Player/PlayerController.swift Sources/UI/PlayerScreen.swift Sources/Core/AppIdentity.swift project.mdklab.yml
          if git diff --cached --quiet; then
            echo 'Build89 source already materialized.'
          else
            git commit -m 'fix: isolate MDK direct HTTP and hide premature EOF banner'
            git fetch origin feat/recovery-isolation-0.13.17
            git rebase origin/feat/recovery-isolation-0.13.17
            git push origin HEAD:feat/recovery-isolation-0.13.17
          fi

      - name: Upload final test package
        uses: actions/upload-artifact@v4
        with:
          name: OnePlayer-0.13.22-build89-mdk-direct-http-silent-eof
          path: |
            OnePlayer-0.13.22-build89-mdk-direct-http-silent-eof-unsigned.ipa
            OnePlayer-0.13.22-build89-mdk-direct-http-silent-eof-unsigned.ipa.sha256
            app-validation.log
            min-os-report.log
          if-no-files-found: error
          retention-days: 14
