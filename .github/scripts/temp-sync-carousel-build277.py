from pathlib import Path
import re
import sys

root = Path(sys.argv[1]) if len(sys.argv) > 1 else Path('.')

checkpoint = root / 'docs/project/current/dev/DEV-home-carousel-drag-smoothness.md'
text = checkpoint.read_text()
text = re.sub(r'^- \*\*Status:\*\*.*$', '- **Status:** Active — Build275 same-package target-device control is now `CAROUSEL≈90` while `TREE FULL / TREE HERO / TREE BACKDROP / CA / DISPLAYLINK / SWIFTUI = 120` with screen recording off. This proves the unchanged full carousel presentation has 120 Hz headroom under fixed device-max progress and moves the next evidence boundary to the real input→progress-publication path. Build277 / 0.15.10 is the current CI/IPA-verified input-pipeline benchmark.', text, count=1, flags=re.M)
text = re.sub(r'^- \*\*Current working branch:\*\*.*$', '- **Current working branch:** `diag/home-carousel-input-pipeline-build277`.', text, count=1, flags=re.M)
text = re.sub(r'^- \*\*Current exact product source:\*\*.*$', '- **Current exact product source:** `1446640b0d9cec5cb2f39d36cff0bfeca4efd31d`.', text, count=1, flags=re.M)
text = re.sub(r'^- \*\*Current candidate:\*\*.*$', '- **Current candidate:** OnePlayer `0.15.10 (277)` — diagnostic-only `TOUCH LAYER / PAN LAYER / SCROLLVIEW` input benchmark; exact-source CI passed and IPA independently verified; target-device input HUD comparison pending.', text, count=1, flags=re.M)
insert = '''\n## Build275 same-package control result — 2026-08-31\n\nThe user completed the missing normal control in the **same Build275 package** with screen recording off: **`PIPE CAROUSEL ≈90 FPS`**. The same package/session family already showed **`TREE FULL=120 / TREE HERO=120 / TREE BACKDROP=120 / CA=120 / DISPLAYLINK=120 / SWIFTUI=120`** through the real system FPS HUD. This supersedes the earlier attempt to treat Build274 `TREE FULL≈90` as a stable full-tree ceiling: that result did not reproduce in Build275 and must not remain the causal foundation.\n\nThe decisive boundary is now input/publication rather than steady-state presentation cost. In exact Build275 source, normal manual drag writes `transitionProgress` only when the custom UIKit recognizer receives a delivered `touchesMoved` callback; the diagnostic max-refresh `CADisplayLink` running during drag only records cadence and does not drive the carousel. Historical device logs also show delivered-touch/progress-publication/render-change cadence materially sparser than the near-120 display-link callbacks. Do not re-open blur, Hero/backdrop residency, generic SwiftUI/CALayer capability or full-tree component-removal directions from this evidence.\n\n## Build277 / 0.15.10 — input-pipeline benchmark\n\nBuild277 branches from exact Build275 source and changes only `Sources/Core/AppIdentity.swift` plus diagnostic `Sources/UI/EmbyHomeFramePipelineProbeV3.swift`. `EmbyHomeCoreV3.swift`, `EmbyHomeHeroV3.swift`, `EmbyHomeCarouselInteractionV3.swift`, and `EmbyHomeCarouselStateV3.swift` remain exact Build275 blobs `ae9e98c09bd10294032d08382b53a18f31bf42ac`, `ab2ab5d80a59e174622dca0006c0f3aad4111a54`, `f8df5af61101c0272c5ec378caae617000b8fcea`, and `96f38514cfb09668f11c21a61105ac87a2f26f3d`.\n\nNew black-screen diagnostic modes compare the input layer while avoiding carousel-tree cost: `TOUCH LAYER` is custom `UIGestureRecognizer.touchesMoved → CALayer`, `PAN LAYER` is `UIPanGestureRecognizer → CALayer`, and `SCROLLVIEW` is native horizontal `UIScrollView`. Each input probe carries the same device-max refresh request as the carousel diagnostic so system power-policy downshifting does not contaminate the comparison; the no-op refresh driver does not move the marker or mutate product carousel state. No interpolation, predicted smoothing, timer/watchdog/retry/fallback, product gesture-owner replacement, or P0 change is included.\n\nCorrected authoritative exact product source: `1446640b0d9cec5cb2f39d36cff0bfeca4efd31d`. The earlier source `9402570e31f6a681e817766e1bea81392d739d5e` compiled successfully but lacked the fair max-refresh request in the three input probes and is **not** a real-device baseline. Corrected Xcode 16.4 run/job `33336619261 / 99324579844` passed. Artifact `9739256003`, digest `sha256:06f93adeb462844b49e2c202ec694add5401f68afb49595f94cc3ddc68dbe37d`. IPA SHA-256 `e27c86b5084db257174d3afd5cc33e147be6868ef6262245f8a3361ed63f097c`; source ZIP SHA-256 `32516a75d88f1f98c9fa10159f1ac77772e9aa05098072079538c20bc337e396`. Independent unpack verifies IPA integrity, `com.embyplayerlab.app / OnePlayer / 0.15.10 (277)`, `MinimumOSVersion=15.0`, and MinOS audit OK.\n\nTarget-device procedure: recording off, measure `CAROUSEL`, then drag continuously in `TOUCH LAYER`, `PAN LAYER`, and `SCROLLVIEW`, and report the sustained HUD values plus App log. Example: `CAROUSEL 90 / TOUCH 90 / PAN 90 / SCROLLVIEW 120`.\n'''
if '## Build275 same-package control result — 2026-08-31' not in text:
    text = text.replace('\n## Scope guard\n', insert + '\n## Scope guard\n')
text = re.sub(r'## Acceptance / test procedure\n.*?(?=\n## Validation state)', '''## Acceptance / test procedure\n\n1. Use iPhone 15 Pro Max / iOS 17.0 with screen recording **off**.\n2. `PIPE CAROUSEL`: rapidly swipe as the normal control and note sustained real system HUD.\n3. Tap to `PIPE TOUCH LAYER`: on the black screen, drag the white marker continuously left/right and note HUD.\n4. Tap to `PIPE PAN LAYER`: drag continuously and note HUD.\n5. Tap to `PIPE SCROLLVIEW`: drag/fling the native horizontal content and note HUD.\n6. Send one line such as `CAROUSEL 90 / TOUCH 90 / PAN 90 / SCROLLVIEW 120` plus App log.\n7. Do not infer performance from internal `CADisplayLink` frequency; real HUD with recording off remains authoritative.''', text, flags=re.S)
text = re.sub(r'## Validation state\n.*?(?=\n## Next exact action)', '''## Validation state\n\n- Build265 / 269 / 270: target-device ~90 control/rejected component A/Bs; not stable.\n- Build271: target-device `DISPLAYLINK=120 / SWIFTUI=120`; generic pipeline 120 capability proven.\n- Build274: target-device `CAROUSEL≈90 / TREE FULL≈90`, but TREE≈90 did not reproduce in Build275 and is not stable causal evidence.\n- Build275 Code/CI/IPA: ✅. Real-device: ✅ **same-package `CAROUSEL≈90` while TREE FULL/HERO/BACKDROP/CA/DISPLAYLINK/SWIFTUI=120**; input/publication boundary isolated; diagnostic-only.\n- Build277 Code written: ✅ exact product source `1446640b0d9cec5cb2f39d36cff0bfeca4efd31d`.\n- Build277 exact-source CI passed: ✅ run/job `33336619261 / 99324579844`.\n- Build277 IPA produced + independently verified: ✅ artifact `9739256003`; IPA SHA `e27c86b5084db257174d3afd5cc33e147be6868ef6262245f8a3361ed63f097c`; source SHA `32516a75d88f1f98c9fa10159f1ac77772e9aa05098072079538c20bc337e396`; MinOS 15.0.\n- Build277 target-device input benchmark: ❌ pending.\n- Stable/frozen reopened performance task: ❌.''', text, flags=re.S)
text = re.sub(r'## Next exact action\n.*\Z', '''## Next exact action\n\nInstall corrected Build277 and keep screen recording off. Compare `CAROUSEL → TOUCH LAYER → PAN LAYER → SCROLLVIEW` under continuous horizontal finger motion, then provide the sustained real HUD values and App log. Do not change product gesture ownership, add display-link interpolation, or reopen render-tree component A/Bs before this input-boundary result.''', text, flags=re.S)
checkpoint.write_text(text)

module = root / 'docs/project/MODULE_STATUS.md'
text = module.read_text()
row = '| Home carousel interaction / presentation cadence | **Active — Build275 isolates input/publication boundary; Build277 CI/IPA verified, input benchmark pending** | Same Build275 package with recording off is `CAROUSEL≈90` while `TREE FULL/HERO/BACKDROP/CA/DISPLAYLINK/SWIFTUI=120`, proving the unchanged presentation tree can sustain 120 when driven by device-max progress and moving the next boundary to real input→progress publication. Build277 / 0.15.10 exact source `1446640b0d9cec5cb2f39d36cff0bfeca4efd31d` changes only AppIdentity + diagnostic probe; HomeCore/Hero/Interaction/State remain exact Build275 blobs. Corrected run/job `33336619261 / 99324579844`; artifact `9739256003`; IPA SHA `e27c86b5084db257174d3afd5cc33e147be6868ef6262245f8a3361ed63f097c`; source SHA `32516a75d88f1f98c9fa10159f1ac77772e9aa05098072079538c20bc337e396`; MinOS 15.0. Tests custom touchesMoved→CALayer, UIPan→CALayer and native horizontal UIScrollView with equal max-refresh request. Build241 product/P0 remain protected; diagnostic-only, not stable. |'
text = re.sub(r'^\| Home carousel interaction / presentation cadence \|.*$', row, text, count=1, flags=re.M)
module.write_text(text)

state = root / 'docs/project/PROJECT_STATE.md'
text = state.read_text()
text = re.sub(r'^_Last updated .*?_$', '_Last updated 2026-08-31: Home carousel Build275 same-package target-device control is now `CAROUSEL≈90` while `TREE FULL/HERO/BACKDROP/CA/DISPLAYLINK/SWIFTUI=120` with recording off. The full presentation tree therefore has 120 Hz headroom; the active boundary is real input→progress publication. Build277 / 0.15.10 is exact-source CI/IPA verified and compares custom touch, UIPan and native UIScrollView input pipelines. Poster Build276/Aether remain isolated; all P0 playback/transport contracts stay protected._', text, count=1, flags=re.M)
text = re.sub(r'## Active: Home carousel presented-FPS diagnosis — Build275 / 0\.15\.8\n.*?(?=\n## Completed / frozen: Home carousel interaction)', '''## Active: Home carousel input/publication diagnosis — Build277 / 0.15.10\n\nBuild275 now supplies the decisive same-package target-device control with recording off: normal finger-driven `PIPE CAROUSEL≈90`, while `TREE FULL=120`, `TREE HERO=120`, `TREE BACKDROP=120`, corrected `CA=120`, `DISPLAYLINK=120`, and `SWIFTUI=120`. Therefore the unchanged carousel presentation tree and generic SwiftUI/CALayer paths have real 120 Hz headroom; Build274 `TREE FULL≈90` did not reproduce and is no longer a stable causal foundation.\n\nExact normal interaction source publishes `transitionProgress` from delivered custom-recognizer `touchesMoved` callbacks; the device-max display link used during manual drag only diagnoses cadence. Build277 isolates this boundary without changing HomeCore/Hero/Interaction/State product blobs. Diagnostic modes compare `TOUCH LAYER` (custom touchesMoved→native CALayer), `PAN LAYER` (UIPanGestureRecognizer→native CALayer), and `SCROLLVIEW` (native horizontal UIScrollView), all under an equal device-max refresh request. Exact source `1446640b0d9cec5cb2f39d36cff0bfeca4efd31d`; corrected run/job `33336619261 / 99324579844`; artifact `9739256003`; IPA SHA `e27c86b5084db257174d3afd5cc33e147be6868ef6262245f8a3361ed63f097c`; source SHA `32516a75d88f1f98c9fa10159f1ac77772e9aa05098072079538c20bc337e396`; MinOS 15.0. Real-device input benchmark pending; no product owner/smoothing/fix claim.\n''', text, flags=re.S)
state.write_text(text)

index = root / 'docs/project/BUILD_TEST_INDEX.md'
text = index.read_text()
rows = '''| **Build275 / 0.15.8** | Home-carousel full/tree scope + corrected pipeline control | **Target-device diagnostic tested; same-package input boundary isolated; not stable.** Exact source `8c6a882c03e60e9d2f49e9bc95b09f9e3712577b`; run/job `33334208681 / 99318066653`; artifact `9738555839`; IPA SHA `26229afe7b1cec29ab2bf2cca18c0348fd3337a2d6f996bd2a6b6b07c5bebe64`; MinOS 15.0. With recording off the user reports `CAROUSEL≈90` while `TREE FULL/HERO/BACKDROP/CA/DISPLAYLINK/SWIFTUI=120`. This supersedes Build274 TREE≈90 as stable causal evidence and proves the unchanged presentation tree has 120 Hz headroom under fixed device-max progress. Next boundary is real input→progress publication. |\n| **Build277 / 0.15.10** | Home-carousel input-pipeline benchmark | **Exact-source CI/IPA verified; target-device pending; diagnostic-only.** Corrected exact source `1446640b0d9cec5cb2f39d36cff0bfeca4efd31d` changes only AppIdentity + frame-pipeline probe; HomeCore/Hero/Interaction/State remain exact Build275 blobs. `TOUCH LAYER` compares custom touchesMoved→CALayer, `PAN LAYER` UIPan→CALayer, `SCROLLVIEW` native horizontal UIScrollView; all receive equal device-max refresh request. Corrected run/job `33336619261 / 99324579844`; artifact `9739256003`, digest `sha256:06f93adeb462844b49e2c202ec694add5401f68afb49595f94cc3ddc68dbe37d`; IPA SHA `e27c86b5084db257174d3afd5cc33e147be6868ef6262245f8a3361ed63f097c`; source ZIP SHA `32516a75d88f1f98c9fa10159f1ac77772e9aa05098072079538c20bc337e396`; MinOS 15.0 independently verified. Earlier source `9402570e...` lacked fair refresh request and is not a device baseline. |\n'''
if '| **Build275 / 0.15.8** |' not in text:
    anchor = '| **Carousel Build242 / 0.14.75** |'
    pos = text.find(anchor)
    if pos < 0: raise SystemExit('BUILD_TEST_INDEX anchor missing')
    end = text.find('\n', pos)
    text = text[:end+1] + rows + text[end+1:]
else:
    text = re.sub(r'^\| \*\*Build275 / 0\.15\.8\*\* \|.*$', rows.splitlines()[0], text, count=1, flags=re.M)
    if '| **Build277 / 0.15.10** |' not in text:
        line = rows.splitlines()[0]
        text = text.replace(line + '\n', rows, 1)
index.write_text(text)

decisions = root / 'docs/project/TECHNICAL_DECISIONS.md'
text = decisions.read_text()
d024 = '''\n\n## D024 — Build275 isolates carousel cadence to real input→progress publication\n\nWith screen recording off on the target iPhone 15 Pro Max / iOS 17.0, the same Build275 package presents normal finger-driven `PIPE CAROUSEL≈90 FPS` while `TREE FULL`, `TREE HERO`, `TREE BACKDROP`, corrected `CA`, `DISPLAYLINK`, and `SWIFTUI` all reach a real 120 FPS system HUD. Therefore a generic ProMotion/SwiftUI/CALayer ceiling and the unchanged steady-state carousel presentation tree are not demonstrated causes of the normal ~90 ceiling. Build274 `TREE FULL≈90` did not reproduce and must not remain a stable causal premise.\n\nExact normal source publishes `transitionProgress` only when the custom UIKit recognizer receives a delivered `touchesMoved` callback; its max-refresh `CADisplayLink` is diagnostic-only. The next permitted A/B is therefore input-pipeline attribution before any product owner change or display-link interpolation. Build277 compares custom touchesMoved→native CALayer, UIPanGestureRecognizer→native CALayer, and native horizontal UIScrollView under the same device-max refresh request, while leaving HomeCore/Hero/Interaction/State product blobs unchanged.\n\nDo not re-open blur30, foreground residency, Hero/backdrop scope removal, generic UIKit-vs-SwiftUI rewrites, guessed easing, timer/watchdog/retry/fallback or predictive smoothing from this evidence. Product gesture ownership may change only if Build277 real-device input results directly support that architecture.\n'''
if '## D024 — Build275 isolates carousel cadence to real input→progress publication' not in text:
    text += d024
decisions.write_text(text)
