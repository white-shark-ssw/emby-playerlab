from pathlib import Path

module = Path('docs/project/MODULE_STATUS.md')
text = module.read_text()
old = '| Search page / recommendations | **Active — Build247 target-device rejected; Build248 CI/IPA verified, target-device pending** | Build247 / 0.14.80 was tested on iPhone 15 Pro Max / iOS 17.0 and rejected: the new root-owned Search Dock sat too low/outside the screen and the 60-item startup preloader left recommendations on the spinner. Build248 / 0.14.81 exact CI source `dc601099ded1074fafc0c7a4e000b8c6fd4c7338` keeps root Dock ownership but compensates `geometry.safeAreaInsets.bottom`, and restores the original up-to-3×3 recommendation contract: preload limit 9, each Suggestions request asks only for remaining slots, actual returned type remains hard-whitelisted to Movie/Series, and there is no Search recommendation load-more. Run/job `33259763303 / 99119574495` passed; artifact `9716945819`; IPA SHA-256 `8eb734bb26b77f377314223acbf7306da72ac9254a20586bfc443d59fea940c5`; MinOS 15.0 independently verified. Build248 is not real-device tested or stable yet. |'
new = '| Search page / recommendations | **Active — Build248 target-device: Dock accepted, recommendations blocked; Build249 CI/IPA verified, target-device pending** | Build248 / 0.14.81 target-device evidence accepts the Search Dock position and keyboard behavior, but recommendations still remain on the spinner. Uploaded log `OnePlayer-App-1788018797.log` proves Suggestions calls advance sequentially across libraries every ~2.4–2.7 s rather than hanging on one request. Build249 / 0.14.82 exact CI source `f49ed220367de1ffbf9e9a5aba097d2ce160dac7` filters `UserViews` by real CollectionType (`movies`, `tvshows`, `mixed`) before Suggestions, maps each request to Movie/Series appropriately, preserves the returned-item Movie/Series whitelist, 9-item cap, startup warm and existing image caches, and adds preload diagnostics. Run/job `33261820598 / 99124950794` passed; artifact `9717502081`; IPA SHA-256 `0c62d51d488197b55dbfb98ab104c48404dd0caac77d786523f753c75acbb7a0`; MinOS 15.0 independently verified. Build249 is not real-device tested or stable yet. |'
if old not in text:
    raise SystemExit('Search module row not found')
text = text.replace(old, new, 1).replace('Search Build248, poster and Aether remain separate Active tasks', 'Search Build249, poster and Aether remain separate Active tasks')
module.write_text(text)

blocks = {
'docs/project/PROJECT_STATE.md': '''\n\n## Search Build248 log evidence → Build249 recommendation traversal candidate — 2026-08-29\n\nBuild248 / 0.14.81 is now target-device tested. The user confirms the Search Dock vertical position is correct and focusing the input no longer moves it; preserve this accepted Dock ownership/safe-area behavior. Recommendations still remain on the spinner. Uploaded `OnePlayer-App-1788018797.log` shows Suggestions calls at 15:53:02.153, 04.609, 07.188, 09.961, 12.498, 14.952 and 17.412, so the network is not stuck on a single request: the Search preloader is serially traversing user libraries. Exact Build248 source asks every `userViews` entry for Movie+Series and only publishes after accumulating 9 accepted items or exhausting all views, allowing incompatible/non-video views to consume full request latency while contributing zero accepted items.\n\nBuild249 / 0.14.82 exact CI source `f49ed220367de1ffbf9e9a5aba097d2ce160dac7` changes only the Search recommendation preloader runtime plus changelog. It uses the already-decoded real `CollectionType`, queries only `movies`, `tvshows`, and `mixed`, maps requests to Movie / Series / Movie+Series respectively, preserves the hard returned-item Movie/Series whitelist, 9-item cap, startup one-shot warm and existing image caches, and adds diagnostics for total/eligible views and returned/accepted counts. Run/job `33261820598 / 99124950794` passed; artifact `9717502081`; digest `sha256:3cc924d6733cb4590361fa255d85ef2c31f879f07538e11523a6e246da487510`; IPA SHA-256 `0c62d51d488197b55dbfb98ab104c48404dd0caac77d786523f753c75acbb7a0`; bundle `com.embyplayerlab.app`; MinOS 15.0; IPA integrity independently verified. Evidence: **Code written ✅ / CI passed ✅ / IPA produced+verified ✅ / Build249 real-device pending / not stable**.\n''',
'docs/project/BUILD_TEST_INDEX.md': '''\n\n### Build249 / 0.14.82 — Search recommendation CollectionType traversal candidate\n\n- Build248 target-device: Search Dock position/keyboard behavior accepted; recommendation wall still spins.\n- uploaded log `OnePlayer-App-1788018797.log`: Suggestions requests advance sequentially across parent libraries about every 2.4–2.7 s; not a single hung HTTP call.\n- exact CI product source: `f49ed220367de1ffbf9e9a5aba097d2ce160dac7`.\n- runtime delta: query only `movies`/`tvshows`/`mixed` UserViews; map to Movie/Series request type; keep final actual-type Movie/Series whitelist, 9-item cap, startup warm, existing image caches and no recommendation load-more; add preload diagnostics.\n- run/job: `33261820598 / 99124950794` — success.\n- artifact: `OnePlayer-0.14.82-Build249-Search`; ID `9717502081`; digest `sha256:3cc924d6733cb4590361fa255d85ef2c31f879f07538e11523a6e246da487510`.\n- IPA SHA-256: `0c62d51d488197b55dbfb98ab104c48404dd0caac77d786523f753c75acbb7a0`.\n- independently verified package: `com.embyplayerlab.app`, `0.14.82 (249)`, `MinimumOSVersion=15.0`, IPA `unzip -t` passed.\n- evidence: **Code written ✅ / CI passed ✅ / IPA produced+verified ✅ / target-device pending / not stable.**\n''',
'docs/project/TECHNICAL_DECISIONS.md': '''\n\n## Search recommendation view eligibility — Build249 candidate\n\n- `UserViews` must not be treated as if every view is a Movie/Series recommendation source. Search recommendation traversal uses the real decoded `LibraryItem.collectionType` before issuing Suggestions. Eligible collection types are `movies`, `tvshows`, and `mixed`; request types map to Movie, Series, and Movie+Series respectively.\n- The returned item `Type` remains the final client-visible whitelist authority. CollectionType filtering is request selection, not a replacement for the Movie/Series output whitelist.\n- Build248 target-device evidence accepts the root-owned, bottom-safe-area-compensated Search Dock behavior; Build249 must preserve it.\n- No retry/timer/watchdog/fallback or second image cache is introduced. Build249 remains candidate-level pending target-device validation.\n'''
}
for filename, block in blocks.items():
    path = Path(filename)
    current = path.read_text()
    marker = block.strip().splitlines()[0]
    if marker not in current:
        if not current.endswith('\n'):
            current += '\n'
        current += block
        path.write_text(current)
