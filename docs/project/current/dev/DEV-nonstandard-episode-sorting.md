# DEV-nonstandard-episode-sorting

## Status

**Active**

- **Work ID**：`DEV-nonstandard-episode-sorting`
- **Routing aliases / keywords**：非标准剧集排序 / 剧集排序 / 特殊剧集排序 / episode sorting
- **Task**：修复 OnePlayer 对非标准剧集的排序问题。真机截图、App 日志与 Emby TV episodes API 证据已确认：问题不是标题自然排序或 UI 二次排序，而是 OnePlayer 使用通用 Items 查询并强制 `ParentIndexNumber,IndexNumber`，绕过了 Emby TV series episode order authority。
- **User intent / acceptance criteria**：用户 2026-08-25 提供同一非标准剧集在 OnePlayer 与 EplayerX 的详情页/选集页截图，并明确确认 EplayerX 顺序与 Emby 一致、OnePlayer 当前错误。修复后 OnePlayer 应服从 Emby 实际剧集顺序；不得按标题/文件名/日期/ID 猜排序；标准剧集不得回归；详情页、全部选集、Player 选集与自动下一集必须消费同一 canonical order。
- **Canonical baseline**：当前 real-device accepted / merged functional baseline 为 **OnePlayer 0.14.9 / Build176**，canonical branch `main`。Build178 最终产品 branch 已同步到本任务 CI checkpoint 后的 `main@b34706885f6925bcd4b9f063ef7f2d632eb295ad`；该 main 前进只更新本任务 checkpoint，未改变 Build176 runtime source。
- **Working branch / PR / product head**：branch `fix/nonstandard-episode-sorting`；draft PR **#254**，base=`main`；当前 post-CI-helper clean product head `8718f60a1b0a3d0034473f1cc1769c0b5bc3665f`。相对 `main@b347068...` 只有 `Sources/Core/AppIdentity.swift`、`Sources/Networking/EmbyAPIClient.swift`、`scripts/check_series_episode_ordering.py`、`docs/changelog/CHANGELOG_v0_14_11_build178.md` 四个差异；临时 workflow 已完全恢复，不在最终 diff 中。
- **Build candidate**：**OnePlayer 0.14.11 / Build178** 已分配；artifact `OnePlayer-0.14.11-build178-episode-ordering`。独立首页轮播任务保留 **0.14.10 / Build177**，本任务未复用。
- **Real-device evidence / root cause**：series `137597` 日志：`episodesTotal=165`、`seasonsTotal=5`、季计数 `1=41,2=3,3=31,4=4,5=86`，SeasonId/ParentId 分组一致；`nilIndex=164`，即 165 集中 164 集 `IndexNumber=nil`。`sampleFirst[0...4]` 与 OnePlayer 截图顶部项目完全对应，证明错误顺序来自共享 `seriesEpisodes()` 结果而不是 UI。旧实现调用 `/Users/{UserId}/Items` 并强制 `ParentIndexNumber,IndexNumber`；Emby 专用 `GET /Shows/{Id}/Episodes` 使用 series/season episode authority。
- **Implemented minimal fix**：`seriesEpisodes(seriesId:)` 改用 `Shows/{seriesId}/Episodes`，携带 `UserId`、既有 browse fields/image/user-data、`StartIndex`/`Limit` 分页；不发送 `SortBy`/`SortOrder`，保留服务器顺序、原分页终止条件和 ID-preserving `deduplicated(all)`。没有标题/文件名/DateCreated/ID fallback，没有人工补号，没有 retry/timer/watchdog，没有修改 UI、Player、Transport、Cache 或 Resume。
- **Contract check**：`scripts/check_series_episode_ordering.py` 固定检查 TV episodes path、UserId/分页、禁止 `SortBy` / `ParentIndexNumber,IndexNumber` / 旧 `libraryItems(parentId:seriesId...)`，并保持 iOS 15.0 Deployment Target。
- **Parallel / downstream**：Build176 player episode selector/auto-next 是 accepted main baseline，也是共享 canonical order 的下游消费者。本任务没有修改 `PlayerEpisodeSelection.swift` / `PlayerScreen.swift` / Player session owner；Build178 dedicated CI 明确检查这些 accepted downstream/frozen files 相对 Build176 main 未变化。首页轮播 Build177 与本任务无文件/状态 owner 冲突。
- **Frozen / do-not-touch**：PlayerController、MPV fast Seek、PiP、UnifiedTransport、Range/302/115 客户端直连、Session cache、Cache UI、Emby progress/resume 均未修改。
- **CI / IPA evidence**：dedicated standard MPV Release CI source `db9aa2498fba5c6b092bfec2427042859e32b26a`；run **32836693548** completed **success**。通过 ordering contract、Build176 downstream/frozen diff、Xcode 16.4 dependency resolution、Release compile、OnePlayer **0.14.11 (178)** identity validation、iOS **15.0** MinOS audit、IPA packaging 与 artifact upload。Artifact ID `9558981442`，name `OnePlayer-0.14.11-build178-episode-ordering`，archive digest `sha256:b4f75969157eb929f8bccec50cc9fcaf51025f1696370a7143c20e1d59b8d158`。包内 IPA `OnePlayer-0.14.11-build178-episode-ordering-unsigned.ipa` 的 SHA-256 已复核为 `2e4ed5be2c32535249ea2049a9686f6ac24a217e04535806ee6ee4721e78ba5b`；source zip SHA-256 `eb2059c776a5bdc09d17462a1085a63a1c7fdc5e4a80ea3a4fa18c6b9aefd214`。MinOS 日志确认 App + runtime Mach-O minOS 15.0。
- **CI helper restoration**：临时 helper 在 commit `017d4e396e4c2dfc354ac74409ee27753aa213da` 恢复原 workflow；随后 branch 清理/同步到最新 main，形成 post-helper clean product head `8718f60...`。CI source SHA `db9aa249...` 与最终 clean head 分开记录；产品 blobs/排序实现相同，后者不含 helper。
- **Validation state**：Current wrong behavior = **real-device confirmed**；root cause = source/log/API confirmed；**Code written = yes**；**CI passed = yes**；**IPA produced = yes**；fixed behavior real-device tested = **no**；Stable/frozen = no。
- **Pending**：由用户在 iPhone 15 Pro Max / iOS 17.0 真机安装 Build178，同时验证当前 165 集异常 Series 和至少一个 IndexNumber 正常的标准 Series。异常 Series 应与 Emby/EplayerX 顺序一致；HTTP 日志应显示 `/Shows/137597/Episodes?...` 且无强制 SortBy；Player 选集/自动下一集顺序也需跟随同一 canonical order。只有真机通过后才能接受/合并/关闭本任务。
- **Next exact action**：交付 Build178 IPA 给用户做异常 Series + 标准 Series 双向真机测试；根据真机结果更新 checkpoint / BUILD_TEST_INDEX。若失败，先看 `/Shows/.../Episodes` 实际返回和日志，不加入客户端猜排序 fallback。
- **Rejected / do-not-repeat**：标题自然排序、文件名排序、DateCreated/ID fallback、nil IndexNumber 人工补号、客户端第二套 sort state、旧/新接口 speculative fallback；不要把旧 generic Validate Source 或实验 KSPlayer/MDK workflow 的独立结果当成本 Build178 standard MPV Release 证据。
