# DEV-detail-episode-page-optimization

## Status

**Active**

- **Work ID**：`DEV-detail-episode-page-optimization`
- **Routing aliases / keywords**：详情页优化 / 详情页 / 选集页 / 剧集页 / detail page / episode page
- **Task**：优化 OnePlayer 的媒体详情页与选集/剧集页面。当前第一阶段已由用户真机录屏明确为两项：1）同一剧集详情页退出后再次进入时，不应再次明显经历“文字剧名 → Logo 图片”以及剧集/季等下方内容重新出现的加载过程；2）详情页纵向滑动应消除当前明显的顿挫、抖动和不连续感。
- **User intent / acceptance criteria**：用户 2026-08-25 提供 `进入剧集详情页.mp4` 与 `详情页卡顿，抖动.mp4`。验收重点：首次没有任何本地数据时允许真实网络加载；同一 App 会话内已访问过的详情再次进入应优先显示可安全复用的详情展示数据和已缓存图片，不再先显示文字标题后切 Logo，也不应让已加载的剧集/季/类似内容重新从空态出现；同时仍要后台取得当前 Emby 数据，不能为了“秒开”冻结 Resume/已看/收藏等用户状态。详情纵向滚动必须连续跟手，不出现录屏中的明显停顿、跳动或追帧感。
- **Baseline**：产品运行时基线仍为用户已接受并合并的 OnePlayer 0.14.11 / Build178。本任务 feature branch `feat/detail-episode-page-optimization` 仍基于 `967b743c88d68b05205eb39f1de75cab41362e8b`；之后 `main` 的推进主要包含并行任务 checkpoint/Build179 项目状态更新，最终 CI/合并前必须重新比较真实 runtime source。
- **Working branch / PR / head commit**：branch `feat/detail-episode-page-optimization`；PR = none；当前 branch head 仍为 `967b743c88d68b05205eb39f1de75cab41362e8b`，尚未写产品代码。
- **Build candidate**：none。并行首页轮播任务现已占用 **OnePlayer 0.14.12 / Build179**；本任务后续分配 Build 时必须重新检查 `BUILD_TEST_INDEX.md` 与所有 Active checkpoint，不能复用 Build179。
- **Evidence**：两段用户真机录屏已逐帧检查。重复进入录屏明确显示：详情先以文字剧名（录屏样例“逆局”）进入，随后 Logo 图到达后替换文字；下方剧集相关区域也重新经历异步出现。源码确认 `EmbyMediaDetailView` 每次导航都会创建新的 `EmbyMediaDetailViewModel`；`heroIdentity` 只有 `model.logoImage` 存在时才显示 Logo，而 `logoImage` 来自 `imageInfos`；当前 `load()` 顺序在刷新 item、加载 Series episodes、seasons、media metadata 之后才请求 `client.imageInfos`，因此新 Model 必然先走文字 fallback。另一方面 `EmbyCachedRemoteImage` 已使用 `EmbyDecodedImageRenderPool` 内存缓存 + `EmbyImageDiskCache` 磁盘数据缓存 + 后台 ImageIO decode，所以问题不能简单归因于“图片字节没有缓存”；缺失的是详情展示元数据的 warm state。滑动录屏也明确存在不连续位移；源码进一步确认 `AdaptiveHeroNativeScrollObserver` 直接 KVO `UIScrollView.contentOffset`，详情页当前每次偏移变化超过 0.10pt 就写根 View 的 `@State heroRawScrollMinY`，从而让包含 overview / series / cast / media info / tags / stills / similar 等整棵详情树参与高频失效。项目历史 commit `c1ac9db...` 已对首页相同模式通过独立 Hero scroll state/scope 做过最小隔离，构成直接的同类实现先例。
- **Files / modules in scope**：已确认当前问题的真实主实现为 `Sources/UI/EmbyMediaDetailView.swift`；图片字节缓存实现位于 `Sources/UI/EmbySharedImageAndNavigation.swift`，当前证据表明其已有内存/磁盘缓存，不应为本问题重复新建一套图片缓存；滚动观察器定义在 `Sources/UI/ImmersiveUIComponents.swift`，当前也没有证据需要修改共享观察器。第一版应优先把详情 Hero 高频滚动状态隔离在详情模块内部，并为已成功加载的详情展示元数据提供受限 warm cache，而不是改共享图片/导航或播放器核心。
- **State owner / shared dependencies**：Emby episode canonical order 继续由现有 `/Shows/{SeriesId}/Episodes` 返回顺序拥有；任何详情 warm cache 只能复用该服务器返回数组并由后续真实请求整体刷新，不能新增第二套排序或补号规则。Resume/已看/收藏等用户态仍以 Emby 当前数据和既有 `EmbyUserDataChange` 刷新路径为权威；不得把临时 PlaybackInfo/115/CDN URL/PlaySession 当详情展示缓存。Hero scroll offset 只应由一个详情滚动状态 owner 驱动 Hero 局部渲染。
- **Frozen / do-not-touch**：MPV fast Seek、PiP Build173、UnifiedTransport、Session cache、STRM/302/115 客户端直连、Range/206、Emby Resume/progress、native navigation principle、Player episode session replacement / trusted-natural-end auto-next 均保持现有合同；当前两项问题没有证据要求触碰这些区域。
- **Parallel conflicts checked against**：并行 `DEV-home-carousel-drag-smoothness` 已进入 Build179 candidate，范围仍以首页 `EmbyHomeCarouselStateV3.swift` / `EmbyHomeCoreV3.swift` 为主。本任务第一版若只修改 `EmbyMediaDetailView.swift` 并新增详情专用状态/检查文件，不与首页任务源码 owner 重合；因此应避免为了缓存顺手修改共享 `EmbySharedImageAndNavigation.swift`。最终 CI/合并前仍需重新扫描 current/dev 与 main advancement。
- **Completed**：完成任务路由/独立 branch；读取项目当前权威资料；检查两段用户真机录屏；定位真实详情页、ViewModel、图片缓存、滚动观察器与 API 调用顺序；确认“再进仍文字→Logo”的根因是新 ViewModel 缺少 warm metadata，而非没有图片字节缓存；确认详情滚动把高频 contentOffset 写入根 `@State` 是当前最直接的结构性顿挫来源；确认首页已有同类高频状态隔离先例。
- **Validation state**：Current wrong behavior = **real-device confirmed**；entry reload root cause = **source confirmed**；scroll invalidation root cause = **source confirmed**；Code written = no；CI passed = no；IPA produced = no；fixed behavior real-device tested = no；Stable/frozen = no。
- **Pending**：实现第一版最小修正：1）详情页保留一份有界、仅用于 warm presentation 的已加载元数据快照，使再次进入可立即恢复 Logo image info、episodes、seasons、similar items 等安全展示数据，同时仍执行真实网络刷新；不缓存 PlaybackInfo/临时媒体 URL；2）把 `heroRawScrollMinY` 从详情根 `@State` 移到详情专用 ObservableObject，并只让 Hero subtree 观察它，保持原 stretch/crop/pin 数学与原 UIScrollView 原生滚动；3）增加静态合同检查，禁止高频详情 offset 回到根 `@State`，并保证 canonical episode order / iOS 15.0 / Frozen 路径不变。
- **Next exact action**：在 `feat/detail-episode-page-optimization` 上完成上述两项最小实现，先做源码/静态检查与 branch diff 审查；Build 编号在实现稳定并重新检查并行占用后再分配。若 Hero 状态隔离后真机仍有明显顿挫，下一证据点才考虑 full-screen persistent backdrop blur/compositing 成本，不提前同时重写 GPU 视觉管线。
- **Rejected / do-not-repeat**：不要再加一套图片字节缓存；不要因为 Logo 慢就缓存/复用 PlaybackInfo、PlaySession 或 115/CDN 临时 URL；不要用 debounce/throttle 降低手指滚动响应；不要改 episode canonical order；不要为了滚动性能关闭原生 ScrollView 或重写整个详情架构；不要在第一版同时修改 full-screen blur、图片解码、导航与播放器而导致无法归因。
- **Open questions / risks**：warm snapshot 中的 `LibraryItem.UserData` 可能短暂旧于服务器，因此 warm data 只能作为首帧展示并必须继续发起真实刷新；真机验收需特别观察 Resume/已看/收藏是否及时更新。录屏为 30fps，可证明视觉顿挫但不能单独给出设备真实 120Hz frame-time；性能结论仍必须由修正后的 iPhone 15 Pro Max / iOS 17.0 真机对比确认。
