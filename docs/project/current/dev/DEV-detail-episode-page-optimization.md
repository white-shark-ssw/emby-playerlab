# DEV-detail-episode-page-optimization

## Status

**Active**

- **Work ID**：`DEV-detail-episode-page-optimization`
- **Routing aliases / keywords**：详情页优化 / 详情页 / 选集页 / 剧集页 / detail page / episode page
- **Task**：优化 OnePlayer 的媒体详情页与选集/剧集页面。当前第一阶段已由用户真机录屏明确为两项：1）同一剧集详情页退出后再次进入时，不应再次明显经历“文字剧名 → Logo 图片”以及剧集/季等下方内容重新出现的加载过程；2）详情页纵向滑动应消除当前明显的顿挫、抖动和不连续感。
- **User intent / acceptance criteria**：用户 2026-08-25 提供 `进入剧集详情页.mp4` 与 `详情页卡顿，抖动.mp4`。首次没有任何本地详情数据时允许真实网络加载；同一 App 会话内已访问过的详情再次进入应优先恢复可安全复用的详情展示元数据和既有图片缓存，不再先显示文字标题后切 Logo，也不应让已加载的剧集/季/类似内容从空态重新出现；后台仍必须取得当前 Emby 数据，不能为了“秒开”冻结 Resume/已看/收藏状态。详情纵向滚动必须连续跟手，不出现录屏中的明显停顿、跳动或追帧感。
- **Baseline**：产品运行时基线仍为用户已接受并合并的 OnePlayer 0.14.11 / Build178。本任务 feature branch `feat/detail-episode-page-optimization` 基于 accepted Build178 runtime `main@967b743c88d68b05205eb39f1de75cab41362e8b`。当前 main 已推进到 `f1f23c478daf40c5cde15dc218cf8c64de357709`，推进内容为并行任务/本任务项目状态记录；Build179 首页候选仍未真机接受，因此不替代 Build178 为本任务 runtime baseline。
- **Working branch / PR / head commit**：branch `feat/detail-episode-page-optimization`；PR = none；当前 head `66f1348c5b2052eb669106d535560db468b36f9d`。产品实现已写，当前相对 Build178 base 仅涉及详情页本体、新增详情性能状态文件和静态检查；Build identity/changelog 尚未写入。
- **Build candidate**：**OnePlayer 0.14.13 / Build180** 现分配给本任务；目标 artifact `OnePlayer-0.14.13-build180-detail-page-performance`。分配前已重新检查 `BUILD_TEST_INDEX.md` 与全部 current/dev：Build179 由 `DEV-home-carousel-drag-smoothness` 占用，除此之外无其他 Active Build candidate，Build180 未被占用。从本记录起 Build180 视为本任务保留，其他并行任务不得复用。
- **Evidence**：两段用户真机录屏明确显示重复进入时先出现文字剧名（样例“逆局”）再切换 Logo，下方剧集相关区域也重新异步出现；详情纵向滑动有明显不连续。源码确认 `EmbyCachedRemoteImage` 已有 decoded-memory + disk-data cache，因此不需要第二套图片字节缓存。真正缺失的是新 `EmbyMediaDetailViewModel` 的 warm display metadata：Logo 存在性要等 `imageInfos` 才知道，而旧 `load()` 又在 episodes/seasons/media metadata 之后才请求它。滚动侧确认 `AdaptiveHeroNativeScrollObserver` KVO 原生 `UIScrollView.contentOffset`，旧实现每次偏移变化都写详情根 `@State heroRawScrollMinY`，使整棵详情树高频失效；首页已有同类局部状态隔离先例。当前代码已按这两个直接证据完成第一版最小结构修正。
- **Files / modules in scope**：当前产品改动仅 `Sources/UI/EmbyMediaDetailView.swift`、新增 `Sources/UI/EmbyDetailPerformanceState.swift`；新增 `scripts/check_detail_page_performance.py` 做静态合同。`Sources/UI/EmbySharedImageAndNavigation.swift`、`Sources/UI/ImmersiveUIComponents.swift`、`Sources/Networking/EmbyAPIClient.swift` 均未修改。`project.yml` 已确认标准目标 `sources: - path: Sources` 会自动纳入新增 Swift 文件，Deployment Target 仍为 iOS 15.0。
- **State owner / shared dependencies**：新增 `EmbyDetailHeroScrollState` 是详情 Hero offset 唯一高频渲染 owner，只由 `EmbyDetailHeroScrollScope` 观察；根详情 View 只持有引用，不订阅每帧 objectWillChange。warm presentation 使用系统 `NSCache`，key 为 server base URL + Emby userId + itemId；仅保存 `episodes / seasons / imageInfos / similarItems`，每次新详情 Model 命中后先恢复首帧展示，同时 `load()` 仍执行原真实 Emby 请求并整体替换这些数组。Emby episode canonical order 仍由 `/Shows/{SeriesId}/Episodes` 返回顺序拥有，不新增排序/补号 owner。PlaybackInfo、MediaSource、PlaySession、ResolvedPlaybackSource、115/CDN 临时 URL 均不进入 warm cache。
- **Frozen / do-not-touch**：MPV fast Seek、PiP Build173、UnifiedTransport、Session cache、STRM/302/115 客户端直连、Range/206、Emby Resume/progress、native navigation principle、Player episode session replacement / trusted-natural-end auto-next 均保持现有合同；当前实现没有修改这些文件/owner。
- **Parallel conflicts checked against**：当前仅另有 `DEV-home-carousel-drag-smoothness` Active，Build179 candidate 范围为首页 `EmbyHomeCarouselStateV3.swift` / `EmbyHomeCoreV3.swift` 等；本任务当前详情源码不与其 product files/state owner 重合。Build180 分配也与 Build179 唯一隔离。最终 CI/IPA/merge 前仍需重新检查 main 和 current/dev 是否继续变化。
- **Completed**：完成任务路由和独立 branch；分析两段真机录屏；定位详情 ViewModel、图片缓存、scroll observer、API 调用顺序；确认重复 Logo/下方加载与滚动顿挫的源码根因；写入第一版实现：1）详情 Hero 每帧 offset 从根 `@State` 移到独立 ObservableObject + Hero-only scope，原 stretch/crop/pin 数学和原生 UIScrollView 不变；2）新增详情 warm presentation `NSCache`，二次进入可立即恢复 Logo image info、episodes、seasons、similar items，随后仍真实刷新；3）播放停止后的 episode UserData 刷新成功后同步更新 warm snapshot；4）增加 `check_detail_page_performance.py`，约束高频状态隔离、warm cache 字段、实时刷新路径、iOS 15.0 和禁止播放/session 数据进入 warm cache；5）确认 XcodeGen 标准 target 自动包含 `Sources` 下新增文件。相对原 base 的 GitHub compare 已确认修改范围收敛，没有播放器/Transport/Cache/Navigation 改动。
- **Validation state**：Current wrong behavior = **real-device confirmed**；root causes = **source confirmed**；Code written = **yes**；source/diff review = **passed first round**；local static script execution = unavailable because当前执行环境无法解析 github.com；CI passed = no；IPA produced = no；fixed behavior real-device tested = no；Stable/frozen = no。
- **Pending**：写 Build180 AppIdentity 与 changelog；建立临时 dedicated standard MPV Release workflow，在 CI 中真实运行 detail performance check、现有 Build178 episode-ordering/accepted-contract checks、Frozen file diff、Xcode 16.4 Release compile、0.14.13 (180) identity、iOS 15.0 MinOS 与 IPA packaging。CI 后必须恢复临时 helper，并记录 CI source/run/artifact。成功只代表 CI passed / IPA produced；最终由用户在 iPhone 15 Pro Max / iOS 17.0 对比验证二次进入与纵向滚动。
- **Next exact action**：写 OnePlayer 0.14.13 / Build180 AppIdentity + changelog，随后准备 dedicated Build180 workflow；CI 通过后恢复 helper并交付 IPA 供真机复测。重点真机检查：同一剧第二次进入是否直接保持 Logo/剧集区；Resume/已看/收藏刷新是否仍正确；持续慢拖、快速甩动、上下反复滑动是否仍有顿挫。若第一版状态隔离后仍明显卡顿，下一证据点才是 persistent full-screen `.blur(radius: 30)` 合成成本，不提前同时改 GPU 视觉管线。
- **Rejected / do-not-repeat**：不要新增第二套图片字节缓存；不要缓存/复用 PlaybackInfo、PlaySession、MediaSource、ResolvedPlaybackSource 或 115/CDN 临时 URL；不要用 debounce/throttle 降低滚动响应；不要改 episode canonical order；不要为了滚动性能替换原生 ScrollView 或重写详情架构；不要在第一版同时修改 full-screen blur、图片解码、导航与播放器而无法归因；warm cache 不使用拍脑袋固定条目数，交由 `NSCache` 系统内存压力策略管理。
- **Open questions / risks**：warm episodes 的 `LibraryItem.UserData` 在首帧可能短暂旧于服务器，所以 warm data 只作为展示快照，真实 load 仍是最终 authority；真机验收需特别观察 Resume/已看/收藏。录屏 30fps 可证明视觉顿挫但不能直接代表 iPhone 120Hz frame-time。当前首次改动尚未经过 Xcode 编译；Swift/UI 行为必须以 Build180 CI 和目标真机为准。
