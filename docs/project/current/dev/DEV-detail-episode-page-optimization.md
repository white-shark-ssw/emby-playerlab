# DEV-detail-episode-page-optimization

## Status

**Active**

- **Work ID**：`DEV-detail-episode-page-optimization`
- **Routing aliases / keywords**：详情页优化 / 详情页 / 选集页 / 剧集页 / detail page / episode page
- **Task**：优化 OnePlayer 的媒体详情页与选集/剧集页面。当前第一阶段已由用户真机录屏明确为两项：1）同一剧集详情页退出后再次进入时，不应再次明显经历“文字剧名 → Logo 图片”以及剧集/季等下方内容重新出现的加载过程；2）详情页纵向滑动应消除当前明显的顿挫、抖动和不连续感。
- **User intent / acceptance criteria**：用户 2026-08-25 提供 `进入剧集详情页.mp4` 与 `详情页卡顿，抖动.mp4`。首次没有任何本地详情数据时允许真实网络加载；同一 App 会话内已访问过的详情再次进入应优先恢复可安全复用的详情展示元数据和既有图片缓存，不再先显示文字标题后切 Logo，也不应让已加载的剧集/季/类似内容从空态重新出现；后台仍必须取得当前 Emby 数据，不能为了“秒开”冻结 Resume/已看/收藏状态。详情纵向滚动必须连续跟手，不出现录屏中的明显停顿、跳动或追帧感。
- **Baseline**：产品运行时基线仍为用户已接受并合并的 OnePlayer 0.14.11 / Build178。本任务 feature branch `feat/detail-episode-page-optimization` 基于 accepted Build178 runtime `main@967b743c88d68b05205eb39f1de75cab41362e8b`。Build179/Build180 首页轮播线未替代 Build178 accepted runtime。
- **Working branch / PR / head commit**：branch `feat/detail-episode-page-optimization`；PR = none。详情功能源码已写并经过一轮 dedicated CI 编译验证；当前将仅重发唯一版本身份，不改详情功能实现。
- **Build candidate**：**OnePlayer 0.14.14 / Build181** 现正式保留给本任务；目标 artifact `OnePlayer-0.14.14-build181-detail-page-performance`。原因：本任务曾先预留并成功构建 0.14.13 / Build180，但并行 `DEV-home-carousel-drag-smoothness` 后续也占用了 0.14.13 / Build180，形成同号不同产品的治理冲突。为避免日志、录屏和 IPA 身份歧义，详情 Build180 不作为用户测试基线，顺延为 Build181。当前全部 `current/dev` 仅有详情与首页轮播两个 Active 任务，Build181 未被其他任务占用；从本记录起其他并行任务不得复用 Build181。
- **Evidence**：两段用户真机录屏明确显示重复进入时先出现文字剧名（样例“逆局”）再切换 Logo，下方剧集相关区域也重新异步出现；详情纵向滑动有明显不连续。源码确认 `EmbyCachedRemoteImage` 已有 decoded-memory + disk-data cache，因此不需要第二套图片字节缓存。真正缺失的是新 `EmbyMediaDetailViewModel` 的 warm display metadata：Logo 存在性要等 `imageInfos` 才知道，而旧 `load()` 又在 episodes/seasons/media metadata 之后才请求它。滚动侧确认 `AdaptiveHeroNativeScrollObserver` KVO 原生 `UIScrollView.contentOffset`，旧实现每次偏移变化都写详情根 `@State heroRawScrollMinY`，使整棵详情树高频失效；首页已有同类局部状态隔离先例。当前代码已按这两个直接证据完成第一版最小结构修正。
- **Files / modules in scope**：产品改动仅 `Sources/UI/EmbyMediaDetailView.swift`、新增 `Sources/UI/EmbyDetailPerformanceState.swift`、`Sources/Core/AppIdentity.swift`；静态合同为 `scripts/check_detail_page_performance.py`，并按新局部状态 owner 更新了 `scripts/check_adaptive_hero_reveal.py` 中一条过时结构断言。`Sources/UI/EmbySharedImageAndNavigation.swift`、`Sources/UI/ImmersiveUIComponents.swift`、`Sources/Networking/EmbyAPIClient.swift` 均未修改。标准 target 自动包含 `Sources`，Deployment Target 仍为 iOS 15.0。
- **State owner / shared dependencies**：新增 `EmbyDetailHeroScrollState` 是详情 Hero offset 唯一高频渲染 owner，只由 `EmbyDetailHeroScrollScope` 观察；根详情 View 只持有引用，不订阅每帧 objectWillChange。warm presentation 使用系统 `NSCache`，key 为 server base URL + Emby userId + itemId；仅保存 `episodes / seasons / imageInfos / similarItems`，每次新详情 Model 命中后先恢复首帧展示，同时 `load()` 仍执行原真实 Emby 请求并整体替换这些数组。Emby episode canonical order 仍由 `/Shows/{SeriesId}/Episodes` 返回顺序拥有，不新增排序/补号 owner。PlaybackInfo、MediaSource、PlaySession、ResolvedPlaybackSource、115/CDN 临时 URL 均不进入 warm cache。
- **Frozen / do-not-touch**：MPV fast Seek、PiP Build173、UnifiedTransport、Session cache、STRM/302/115 客户端直连、Range/206、Emby Resume/progress、native navigation principle、Player episode session replacement / trusted-natural-end auto-next 均保持现有合同；当前实现没有修改这些文件/owner。
- **Parallel conflicts checked against**：`DEV-home-carousel-drag-smoothness` 当前占用 OnePlayer 0.14.13 / Build180，范围为首页 carousel owner；详情源码 owner 与其不重合。Build181 已重新检查并正式保留，解决 Build180 身份碰撞。
- **Completed**：完成任务路由和独立 branch；分析两段真机录屏；定位详情 ViewModel、图片缓存、scroll observer、API 调用顺序；确认重复 Logo/下方加载与滚动顿挫的源码根因；实现 Hero 高频状态局部化和详情 warm presentation `NSCache`；增加静态合同；第一轮 0.14.13 / Build180 dedicated CI run `32843951020` 已完整通过 detail/inherited contracts、Xcode 16.4 Release compile、App identity、iOS 15.0 MinOS、IPA packaging 和 artifact upload，artifact ID `9561639915`，IPA SHA-256 `53fec4411038cae6fbff3c4f6ec827954d5a98674d95892c0095e6aec46f9b54`。该包因与并行首页 Build180 同号而作废为用户测试基线，但证明当前详情功能源码可通过标准 Release 构建。
- **Validation state**：Current wrong behavior = **real-device confirmed**；root causes = **source confirmed**；Code written = **yes**；first Release compile/MinOS/package = **passed on conflicted Build180 identity**；Build181 CI passed = no；Build181 IPA produced = no；fixed behavior real-device tested = no；Stable/frozen = no。
- **Pending**：仅将详情候选身份顺延到 OnePlayer 0.14.14 / Build181，功能源码保持不变；重新运行 dedicated standard MPV Release workflow，验证相同 detail performance / Hero / detail / episode-ordering / Frozen contracts、Xcode 16.4 Release、0.14.14 (181) identity、iOS 15.0 MinOS 与 IPA packaging。成功后交付 Build181 IPA 给用户真机复测，并记录 CI source/run/artifact/SHA；不能把 CI/IPA 描述成真机问题已解决。
- **Next exact action**：更新 AppIdentity/changelog/dedicated workflow 为 0.14.14 / Build181 并重新 CI；拿到唯一身份 IPA 后立即交付。真机重点检查：同一剧第二次进入是否直接保持 Logo/剧集区；Resume/已看/收藏刷新是否仍正确；持续慢拖、快速甩动、上下反复滑动是否仍有顿挫。若状态隔离后仍明显卡顿，下一证据点才是 persistent full-screen `.blur(radius: 30)` 合成成本。
- **Rejected / do-not-repeat**：不要新增第二套图片字节缓存；不要缓存/复用 PlaybackInfo、PlaySession、MediaSource、ResolvedPlaybackSource 或 115/CDN 临时 URL；不要用 debounce/throttle 降低滚动响应；不要改 episode canonical order；不要为了滚动性能替换原生 ScrollView 或重写详情架构；不要在第一版同时修改 full-screen blur、图片解码、导航与播放器而无法归因；不要继续分发身份冲突的详情 Build180。
- **Open questions / risks**：warm episodes 的 `LibraryItem.UserData` 在首帧可能短暂旧于服务器，所以 warm data 只作为展示快照，真实 load 仍是最终 authority；真机验收需特别观察 Resume/已看/收藏。录屏 30fps 可证明视觉顿挫但不能直接代表 iPhone 120Hz frame-time。最终性能结论必须以 Build181 在 iPhone 15 Pro Max / iOS 17.0 真机测试为准。
