# DEV-home-carousel-drag-smoothness

## Status

**Active**

- **Work ID**：`DEV-home-carousel-drag-smoothness`
- **Routing aliases / keywords**：轮播图滑动卡顿 / 轮播图丝滑 / 首页轮播 / carousel drag / carousel smoothness
- **Task**：优化 OnePlayer 首页 V3 轮播图的手动横向拖动体验，使手指持续拖动期间的画面位移连续、跟手，不出现明显冻结、追帧、抖动、跳变或掉帧感，并以用户提供的 EX 录屏作为体验对照。
- **User intent / acceptance criteria**：用户 2026-08-25 提供 `op轮播图.mp4` 与 `ex轮播图.mp4` 真机录屏。OP 在手动拖动轮播图过程中可明显感受到卡顿/抖动；目标是达到 EX 相同级别的滑动丝滑程度。验收重点：1）手指持续移动时 Banner 位移连续并紧跟手指；2）不能出现明显“停一帧/几帧后突然追上”的感觉；3）不能有可感知抖动、跳变、掉帧感；4）松手后的完成/取消吸附仍自然；5）不得为此破坏首页纵向滚动、轮播自动切换、详情点击、媒体行或播放器/P0 合同。
- **Baseline**：真实功能基线为用户已接受的 Build173 / OnePlayer 0.14.6：branch `fix/pip-seek-completion-return-simplify-0.14.6`，head `4f7acf8da06ded00db735b07210983a0d2dd5be6`，PR #238。任务最初误从 `main@49f09c13887186eae5117f63deb08f7422248c24` 建 branch，CI 前已按项目规则纠正；核对确认 Build173 与 main 的 `EmbyHomeCarouselStateV3.swift` / `EmbyHomeCoreV3.swift` 原始 blob SHA 完全一致，因此问题实现本身未分叉，但整体仓库已 diverged，不能用 main 作为测试包基线。
- **Working branch / PR / head commit**：branch `perf/home-carousel-drag-smoothness`；已 force-reset 到 Build173 head 后重放本任务优化提交。当前产品/版本/changelog head `ab2aff444fee6d95b6be8b8b085e9885fab670da`；PR = none。相对 Build173 的预期长期差异为 `Sources/UI/EmbyHomeCarouselStateV3.swift`、`Sources/UI/EmbyHomeCoreV3.swift`、`Sources/Core/AppIdentity.swift`、`scripts/check_home_immersive_carousel.py`、`docs/changelog/CHANGELOG_v0_14_10_build177.md`；临时 CI helper 不属于最终产品差异，构建后必须恢复。
- **Build candidate**：**OnePlayer 0.14.10 / Build177** 已分配给本任务；目标 artifact `OnePlayer-0.14.10-build177-home-carousel-smoothness`。分配前已重新检查 `BUILD_TEST_INDEX.md`、`DEV-player-episode-picker` 与 `DEV-nonstandard-episode-sorting`：Build176 已属于选集任务，另一个 Active 任务尚未分配 Build，Build177 当时未被占用。Build177 现在视为本任务保留，其他 Active 任务不得复用。
- **Evidence**：录屏逐帧对比显示 OP 手拖期间存在“持续移动 → 短暂停住 → 下一帧大幅追赶”的视觉节奏，而 EX 的位移更连续。真实源码进一步确认：原实现 `carouselDragGesture(width:)` 在每个 `DragGesture.onChanged` 同时写顶层 `@State transitionProgress`，并且每次都调用 `suppressCarouselTap()` 再写顶层 `@State carouselTapSuppressedUntil`；`V3EmbyHomeView` 根 View 同时包含全首页 ScrollView / media rows / header / dock，因此高频手指事件会无必要地使首页根树失效。项目现有 `V3HomeHeroScrollState` 已用独立 `ObservableObject` scope 隔离高频纵向 Hero scroll 更新，提供了直接的同类架构先例。渲染端同一 transition progress 仍只驱动必要的清晰 Hero 与 persistent blurred backdrop；图片解码本身已在 utility task + decoded cache 中执行，因此没有证据支持先改图片缓存/解码。
- **Files / modules in scope**：当前实际产品修改为 `Sources/UI/EmbyHomeCarouselStateV3.swift`、`Sources/UI/EmbyHomeCoreV3.swift`；Build177 身份仅修改 `Sources/Core/AppIdentity.swift`；静态合同检查修改 `scripts/check_home_immersive_carousel.py`；新增 Build177 changelog。`EmbyHomeHeroV3.swift` 与 `EmbySharedImageAndNavigation.swift` 已读取但本轮没有修改，因为现有证据不足以先动 blur/image pipeline。
- **State owner / shared dependencies**：第一版修正保持单一 carousel interaction owner：新增 `V3HomeCarouselTransitionState`，由 `V3EmbyHomeView` 以 `@State` 持有引用本身，但内部 transition progress/from/to/direction 只由两个 `V3HomeCarouselTransitionScope` 观察；`isDragging` 与 tap suppression 为事件状态，不再作为根 View `@State` 触发重绘。没有建立第二套 progress，也没有 reconciliation/fallback。current item 仍由原 `currentCarouselItemID` 所有，settle 语义不变。
- **Frozen / do-not-touch**：`PlayerController.swift`、MPV fast Seek、PiP Build173、UnifiedTransport、Range/302/115 客户端直连、Session cache、Cache UI、Emby 播放进度/Resume、native navigation 均不在本任务范围。不得引入时间→字节比例 Seek、retry/watchdog/fallback 或与首页轮播无关的重构。
- **Parallel conflicts checked against**：已检查 `DEV-player-episode-picker` 与 `DEV-nonstandard-episode-sorting`。前者范围在 Player 选集 UI/session，后者范围在 Emby 剧集排序数据链；当前均未占用 `EmbyHomeCarouselStateV3.swift` / `EmbyHomeCoreV3.swift` 或首页轮播状态 owner，因此本任务可以独立并行。最终 CI/IPA 前仍需重新扫描 current/dev，避免其他会话后续扩展到相同文件。
- **Completed**：用户目标与录屏证据确认；真实 carousel drag / Hero / persistent backdrop / image loader / vertical hero state isolation 实现已读取；Build173 基线身份已纠正并验证；第一版最小结构性优化已写：1）把高频 transition 状态从首页根 `@State` 移入单一 `V3HomeCarouselTransitionState`；2）只让 Hero 与 persistent backdrop 的局部 scope 观察 transition 更新，避免每个手指事件重算全首页树；3）tap suppression 与 `isDragging` 改为非渲染事件状态；4）`DragGesture.minimumDistance` 从 12 收到与现有横向判定一致的 4pt，减少起拖死区；5）保留原 commit/cancel 阈值、自动轮播 6s/0.62s、松手完成/取消动画、视觉 blur、详情点击与纵向滚动合同；6）新增静态检查防止高频状态回到根 `@State`；7）Build177 AppIdentity 与 changelog 已写。
- **Validation state**：**Code written = yes**；CI passed = no；IPA produced = no；Real-device tested = no；Stable/frozen = no。源码 compare 已确认优化提交相对 Build173 没有播放器/Transport/Cache/Navigation 差异；尚未把源码级审查描述成 CI 通过。
- **Pending**：用临时 dedicated Build177 standard MPV Release helper 执行轮播合同检查、相关静态脚本、Frozen 文件 diff、Xcode 16.4 Release compile、0.14.10 (177) app identity、iOS 15.0 MinOS、IPA packaging；完成后恢复临时 helper。成功后由用户在 iPhone 15 Pro Max / iOS 17.0 与 EX 录屏并排复测手动持续拖动、反向拖动、短距离拖动、取消/提交吸附和自动轮播期间触摸接管感。若第一版仍有持续掉帧，下一优先证据点才是 persistent full-screen blur/opacity 合成成本；不先猜测性重写图片管线。
- **Next exact action**：写入临时 Build177 dedicated standard MPV Release workflow 并通过本 feature branch push 触发；锁定 CI source SHA/run/artifact 后立即恢复 helper，再同步 CI/IPA 证据。成功只记为 CI passed / IPA produced，不宣称丝滑问题已解决。
- **Rejected / do-not-repeat**：不要把问题简单归因于 `.animation` 时长或 spring 参数并盲调；不要加 debounce/throttle 让手势更新变少，因为目标是更跟手而不是少响应；不要引入 timer/watchdog/retry；不要为了性能改播放器/Transport/Cache；不要同时维护两个 carousel progress 状态；不要在无证据时重写整套首页或换成新架构；不要再从 `main` 直接产出本任务测试包而绕过 Build173 功能基线；不要修改旧通用 `Validate Source` 的 0.13.3/Build69 历史校验来迁就本任务。
- **Open questions / risks**：录屏能证明视觉不连续，但第一版隔离优化的真实帧时间收益仍必须由目标真机验证。persistent backdrop 两张全屏 `.blur(radius: 30)` 图片仍随 progress 交叉合成，但当前尚无独立证据证明它是剩余瓶颈；若真机仍不达 EX，再基于新录屏/性能证据处理，避免一次补丁同时改 CPU invalidation 与 GPU 视觉管线而无法归因。
