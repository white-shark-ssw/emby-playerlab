# DEV-home-carousel-drag-smoothness

## Status

**Active**

- **Work ID**：`DEV-home-carousel-drag-smoothness`
- **Routing aliases / keywords**：轮播图滑动卡顿 / 轮播图丝滑 / 首页轮播 / carousel drag / carousel smoothness
- **Task**：优化 OnePlayer 首页 V3 轮播图的手动横向拖动体验，使手指持续拖动期间的画面位移连续、跟手，不出现明显冻结、追帧、抖动、跳变或掉帧感，并以用户提供的 EX 录屏作为体验对照。
- **User intent / acceptance criteria**：用户 2026-08-25 提供 `op轮播图.mp4` 与 `ex轮播图.mp4` 真机录屏。OP 在手动拖动轮播图过程中可明显感受到卡顿/抖动；目标是达到 EX 相同级别的滑动丝滑程度。验收重点：1）手指持续移动时 Banner 位移连续并紧跟手指；2）不能出现明显“停一帧/几帧后突然追上”的感觉；3）不能有可感知抖动、跳变、掉帧感；4）松手后的完成/取消吸附仍自然；5）不得为此破坏首页纵向滚动、轮播自动切换、详情点击、媒体行或播放器/P0 合同。
- **Current integration baseline**：当前真实 accepted runtime baseline 为 OnePlayer 0.14.11 / Build178，已合并到 `main`。Build179 测试线从 `main@967b743c88d68b05205eb39f1de75cab41362e8b` 建立，该 head 已包含用户接受的 Build176 player episode-selection/session contract 与 Build178 canonical Emby episode ordering。Build179 CI 完成后 `main` 前进到 `2f3209ad0205eb9b885bb62369fb12b18598fbef`，compare 证明唯一新增文件是另一个并行任务 checkpoint `DEV-detail-episode-page-optimization.md`，无运行时源码变化，因此不会使 Build179 CI/IPA 证据失效。
- **Historical Build177 note**：本任务最初在 Build173 / 0.14.6 隔离基线上实现并产出 Build177 / 0.14.10，run `32835837158` 已完整 CI success、artifact `9558658580` 已生成；但在发包前发现 `main` 已经通过用户真机接受推进到 Build178。为避免测试包倒退已接受的 Build176/178 功能，Build177 被视为旧整合基线的 preliminary evidence，不作为最终发给用户的测试包；本轮已将相同最小轮播改动移植到 Build178 accepted baseline 并重新出 Build179。
- **Working branch / PR / head commit**：当前测试 branch `perf/home-carousel-drag-smoothness-build179`，由 `main@967b743c88d68b05205eb39f1de75cab41362e8b` 创建；PR = none；临时 CI helper 已恢复，workflow-restored branch head `839cc0c3506c68e1c04887e438a77575a10fd8a0`。相对 Build178 base 的长期差异仅为 `Sources/UI/EmbyHomeCarouselStateV3.swift`、`Sources/UI/EmbyHomeCoreV3.swift`、`Sources/Core/AppIdentity.swift`、`scripts/check_home_immersive_carousel.py`、`scripts/check_scroll_performance.py`、`docs/changelog/CHANGELOG_v0_14_12_build179.md`。
- **Build candidate**：**OnePlayer 0.14.12 / Build179**。Build179 分配前检查 `BUILD_TEST_INDEX.md`、current/dev 目录和 Build179 repository search；当时无其他 Build179 占用。另一个 Active `DEV-detail-episode-page-optimization` 尚未分配 Build，且 checkpoint 已确认其当前预期文件/state owner 与首页轮播无重叠。
- **Evidence for the code change**：录屏逐帧对比显示 OP 手拖期间存在“持续移动 → 短暂停住 → 下一帧大幅追赶”的视觉节奏，而 EX 的位移更连续。真实源码确认原实现 `carouselDragGesture(width:)` 每个 `DragGesture.onChanged` 都写顶层 `@State transitionProgress`，同时调用 `suppressCarouselTap()` 写顶层 `@State carouselTapSuppressedUntil`；`V3EmbyHomeView` 根 View 同时包含全首页 ScrollView / media rows / header / dock，因此高频手指事件会无必要地使首页根树失效。项目已有 `V3HomeHeroScrollState` 独立 `ObservableObject` scope，为同类高频状态隔离提供直接架构先例。图片解码已经在 utility task + decoded cache 中执行，没有证据支持先改图片缓存/解码。
- **Implementation**：1）新增单一 `V3HomeCarouselTransitionState` owner，承载 transition from/to/progress/direction 与非渲染 drag/tap event state；2）`V3EmbyHomeView` 只持有该 owner 引用，Hero 与 persistent backdrop 通过局部 `V3HomeCarouselTransitionScope` 观察高频 progress；3）根 View 不再持有 `transitionProgress/from/to`、`isCarouselDragging`、`carouselTapSuppressedUntil` 等高频 `@State`；4）`DragGesture.minimumDistance` 从 12 pt 收到与现有横向判定一致的 4 pt；5）原 commit/cancel 阈值、0.22/0.18 秒松手动画、6 秒自动轮播、0.62 秒自动过渡、blur 视觉、详情点击与纵向滚动合同保持不变；6）没有引入 debounce/throttle/retry/fallback/timer/watchdog 或第二套 progress owner。
- **Frozen / inherited contracts**：Build179 继承 Build176 player episode-selection/session behavior 与 Build178 canonical Emby episode ordering；`PlayerController.swift`、MPV fast Seek、PiP Build173、UnifiedTransport、Range/302/115 客户端直连、Session cache、Cache UI、Emby 播放进度/Resume、native navigation 均不在本任务产品 diff。CI 还显式将 `PlayerEpisodeSelection.swift`、`PlayerScreen.swift`、`PlayerControlPanelViews.swift`、`PlayerSettings*.swift` 与 `EmbyAPIClient.swift` 相对 Build178 base 锁为零差异，并执行 `check_series_episode_ordering.py`。
- **Parallel conflict check**：`DEV-detail-episode-page-optimization` 当前预期范围为详情页/剧集 UI，checkpoint 明确记录与首页 `EmbyHomeCarouselStateV3.swift` / `EmbyHomeCoreV3.swift` 没有同一文件或同一 state owner；Build179 CI source 创建后 `main` 的唯一变化也是该 checkpoint 文档，没有 runtime overlap。
- **Build179 CI source / run**：dedicated standard MPV Release CI source `22515402f4d17e1a9b4073c535265b65ba55f52d`；run **`32841344067`**；workflow conclusion **success**。通过内容：carousel state-isolation contract、4 pt drag start、现有横向判定/commit/auto-advance/blur visual contract、home carousel architecture check、scroll performance check、Build178 episode-ordering check、accepted/frozen file zero-diff、Xcode 16.4 dependency resolution、Release device build、app identity 0.14.12 (179)、MinOS、IPA packaging、artifact upload。
- **Build179 artifact**：`OnePlayer-0.14.12-build179-home-carousel-smoothness`；artifact ID **`9560700233`**；artifact archive digest `sha256:5d4b06c3fbcc0515a3ce8293c60701ec8ddd31b6632a7bd4d0fac5717d242a24`；IPA `OnePlayer-0.14.12-build179-home-carousel-smoothness-unsigned.ipa`；IPA SHA-256 **`80f2c70215fe3f1c9323894eedbd22c5f61b29bcfb61e3a6e14115a4b932ddd8`**。下载后本地再次执行 artifact 内 SHA256 校验：IPA = OK，source zip = OK。
- **Compatibility evidence**：CI `check_min_os.sh` 与下载后的 `build179-min-os.log` 均确认 App `MinimumOSVersion: 15.0`、主 executable Mach-O `minOS 15.0`、compatibility audit OK；目标真机仍为 iPhone 15 Pro Max / iOS 17.0。
- **Validation state**：**Code written = yes**；**CI passed = yes**；**IPA produced = yes**；Real-device tested = no；Stable/frozen = no。不得把 CI/IPA 成功描述成“已经达到 EX”或“真机卡顿已解决”。
- **Pending real-device validation**：用户安装 Build179 后与 EX 并排复测：持续慢拖、快速拖、左右反向拖、短距离往返、取消吸附、提交吸附、自动轮播期间触摸接管；重点观察是否还有任何明显不跟手、停住后追帧、抖动、跳变或掉帧感。若第一版仍明显不达 EX，下一优先证据点才是 persistent full-screen 两张 `.blur(radius: 30)` backdrop 随 progress 交叉合成的 GPU 成本，不先猜测性重写图片管线。
- **Next exact action**：等待用户对 Build179 的 iPhone 15 Pro Max / iOS 17.0 真机结果。通过后再决定是否接受/合并并更新 Stable 状态；未通过则基于新录屏/真机证据继续定位，不能因为 CI 绿而提前冻结。
- **Rejected / do-not-repeat**：不要把问题简单归因于 `.animation` 时长或 spring 参数并盲调；不要加 debounce/throttle 让手势更新变少；不要引入 timer/watchdog/retry/fallback；不要为了首页性能改播放器/Transport/Cache；不要同时维护两个 carousel progress 状态；不要无证据重写整套首页或图片管线；不要再给用户旧 Build173 整合基线的 Build177 作为最终测试包。
- **Open questions / risks**：当前 CPU/SwiftUI invalidation 热点有明确源码证据并已隔离，但真实帧时间收益必须由目标真机验证。persistent backdrop 两张全屏 `.blur(radius: 30)` 图片仍随 progress 交叉合成，若 Build179 真机仍掉帧，它是下一层候选而不是当前已证实根因。
