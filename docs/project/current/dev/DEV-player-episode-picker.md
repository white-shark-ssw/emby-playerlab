# DEV-player-episode-picker

## Status

**Active**

- **Work ID**：`DEV-player-episode-picker`
- **Task**：新增播放器“选集”功能：播放器底部增加选集入口，展开横向剧集选择；支持季选择；播放设置增加“自动加载下一集”，并用可信自然结束触发下一集。
- **User intent / acceptance criteria**：Build173 播放器没有选集按钮；新增入口。2026-08-25 用户在 Build174 真机第一版基础上提供 EX 录屏与 OnePlayer 详情页截图，要求选集 UI / 选季 / 交互按录屏重做，同时明确 **OnePlayer 现有底栏按钮布局保持当前位置不变**，不要照搬 EX 在打开选集后让按钮行随剧集容器上移。选集内容直接叠在播放器控制层上，不再使用 Build174 灰色大 Sheet；删除右上角关闭按钮，点击选集内容上方的播放器空白区关闭；删除“选集 + 剧名”标题块。内容使用播放器正常横屏 safe-area 起点，避开刘海 / Dynamic Island 侧边。顶部只保留“第N季⌄”季入口；点击显示轻量季菜单、当前季带勾，选别季只原地筛选列表而不切播放。卡片信息层级按详情页横向剧集卡：174×98 横图；一行剧集标题；最多两行 Emby Overview。2026-08-25 Build175 真机截图进一步确认：剧情简介区域不能再被下方播放器功能按钮视觉穿透；底栏坐标仍保持原位不动；`正在播放` 标识改为缩略图正中央。异常 EOF / 提前结束不得误触发自动下一集。
- **Baseline**：用户 2026-08-25 真机确认 Build173 / OnePlayer 0.14.6 为功能基线。对应 PR #238；branch `fix/pip-seek-completion-return-simplify-0.14.6`；head `4f7acf8da06ded00db735b07210983a0d2dd5be6`。
- **Working branch / PR / current head**：branch `feat/player-episode-picker-0.14.7`；draft PR #249。Build176 产品 source head `f701f0446d65e84fc686f69ec14d60402c94839c`；dedicated CI source commit `221630297dc1080279bb8a3f05d69586461b328c`；临时 CI helper 恢复后的 branch head `4b26a7d3a9826c58bfdddd6aafaeb9eeb5c7c943`。
- **Build candidates**：Build174 = OnePlayer 0.14.7 / Build174，CI run `32776020154` passed，artifact `OnePlayer-0.14.7-build174-episode-picker`，用户已安装并提供第一版 UI 真机反馈。Build175 = OnePlayer 0.14.8 / Build175，dedicated standard MPV Release CI run `32780288067` passed，artifact `OnePlayer-0.14.8-build175-episode-picker-ui` produced；用户已真机测试，确认整体方向但指出 overview 被底部按钮视觉穿透、`正在播放` 未居中，因此 Build175 不接受为稳定 UI。Build176 = OnePlayer 0.14.9 / Build176，dedicated standard MPV Release CI run `32782048086` passed，artifact `OnePlayer-0.14.9-build176-episode-picker-ui` produced；等待用户真机验证。
- **Evidence**：`PlayerControlPanel` 原有 `.episodes` 枚举接入底部栏；`PlayerEpisodeCoordinator` 复用 `libraryItem` / `seriesEpisodes` / `playbackInfo` / `resolvePlaybackSource`；`seriesEpisodes()` 使用 `ParentIndexNumber,IndexNumber` 升序；`PlayerScreen` 用外层全屏 host + keyed `PlayerSessionScreen` 替换完整 source-owned 播放会话；自动下一集只接受现有 `PrematureEOFGuard` 的 non-premature 自然结束。详情页真实 `EmbyMediaDetailView` 横向卡片使用 174×98 图片和剧集标题/简介信息层级；`LibraryItem` 已有 `overview`、`parentIndexNumber`、`indexNumber`。Build175 源码确认选集 overlay 在 `PlayerScreen` 已是 `zIndex(30)`，而底部 controls 没有更高 zIndex；真机穿透的根因因此不是单纯 zIndex 数字不足，而是透明选集区域让下层白色按钮继续可见。Build176 在 `PlayerEpisodeSelection.swift` 仅增加选集面板局部 black fade 作为视觉遮罩，不恢复大 Sheet、不移动底栏；同时将缩略图 `ZStack` 改为 centered alignment 并去掉 badge / spinner 的 leading padding。Build176 dedicated CI 的 selector/frozen-file contract、Xcode 16.4 Release、0.14.9 (176) identity、iOS 15.0 MinOS、IPA packaging 均通过。
- **Files / modules in scope**：`Sources/Core/AppIdentity.swift`、`Sources/UI/PlayerControlPanelViews.swift`、`Sources/UI/PlayerEpisodeSelection.swift`、`Sources/UI/PlayerScreen.swift`、`Sources/UI/PlayerSettings.swift`、`Sources/UI/PlayerSettingsView.swift`、Build174/175/176 changelog。Build176 新增产品差异只在 `PlayerEpisodeSelection.swift` + 版本标识/changelog。
- **State owner / shared dependencies**：完整播放器 source-owned session 切换由外层 `PlayerScreen` host 管理；季筛选只是现有 `episodes` 元数据的 UI 状态，不是新的播放会话 owner。选季不解析媒体源；只有用户点集或可信自然结束才解析目标集播放源。
- **Frozen / do-not-touch**：`PlayerController.swift`、MPV fast Seek、PiP Build173、UnifiedTransport/Range/302/115 客户端直连、Session cache、Cache UI、MPV surface；不得引入时间→字节比例 Seek、timer/watchdog/retry。Build176 dedicated contract 已再次确认这些 Frozen 文件相对 Build173 未变化。
- **Parallel conflicts checked against**：`docs/project/current/dev/` 当前只有本任务一个 Active checkpoint，因此 Build176 未与其他并行功能候选冲突。若后续新增任务触及 `PlayerScreen` / session lifecycle / `PlayerSettings*`，必须重新判定并行冲突。
- **Completed**：Build174 功能链和 IPA；Build174 真机 UI 第一轮反馈；Build175 EX-style selector 重做、标准 MPV Release CI/IPA；Build175 真机截图反馈；Build176 已完成代码：保留底栏按钮坐标，选集 panel 增加由透明到黑色 0.86 的局部底部渐变来压住下层按钮视觉穿透；`正在播放` 与 resolving spinner 居中；AppIdentity 更新 0.14.9；Build176 changelog；dedicated standard MPV Release CI run `32782048086`；0.14.9 (176) identity validation；iOS 15.0 MinOS validation；IPA/source artifact。临时 `.github/workflows/build-mdk-lab.yml` 已恢复为 MDK Lab 内容。
- **Validation state**：Build174：Code written = yes；CI passed = yes；IPA produced = yes；Real-device tested = partial；Stable/frozen = no。Build175：Code written = yes；CI passed = yes（run 32780288067）；IPA produced = yes；**Real-device tested = yes（UI follow-up required）**；Stable/frozen = no。Build176：**Code written = yes；CI passed = yes（run 32782048086）；IPA produced = yes；Real-device tested = no；Stable/frozen = no。**
- **Pending**：用户安装 Build176 真机验证：1）剧情简介区域不再被底部功能按钮视觉穿透；2）底栏按钮坐标没有变化；3）`正在播放` 位于缩略图中央。原 Build175 的季菜单、切季不切播放、手动换集、Resume/Emby 进度、自然结束自动下一集、异常 EOF 不跳集、跨季、电影不显示选集仍继续验证。
- **Next exact action**：用户在 iPhone 15 Pro Max / iOS 17.0 安装 `OnePlayer-0.14.9-build176-episode-picker-ui`，根据真机结果只修有证据的问题；在用户反馈前不得把 Build176 描述为已解决/稳定。
- **Rejected / do-not-repeat**：Build174 的灰色大 Sheet、右上角 X、“选集/剧名”标题已被用户否定；不要把 EX 的“按钮跟随剧集容器上移”照搬到 OnePlayer；不要把 Build175 真机穿透问题误判成单纯提高 `zIndex(30)` 数字即可解决；不得在现有 `PlayerController` 内原地替换 source；不得预解析/长期缓存下一集 115/CDN 临时直链；不得单凭引擎 EOF 无条件切集；不得为旧通用 CI 改历史校验器。
- **Open questions / risks**：Build176 局部渐变遮罩的强度需要真机确认是否既能压住下层按钮又保持 EX-style overlay 感；自动下一集默认开启是否保留仍待完整体验确认。
